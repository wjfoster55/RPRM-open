# Response D

1. For supplied z=-1, the complete missing-port fiber is MANY({(-2,1),(-1,0),(0,-1),(1,-2)}), ordered as (x,y). Each pair lies in the carrier and sums to -1; x=2 would require the excluded y=-3, so the list is complete. Supplying the lawful full row (2,-2,0) leaves no missing ports and gives ONE(()), namely the fiber {()}. The empty tuple is one completion with no coordinates; the empty fiber {} means no lawful completion.

2. The independent marginals do not retain the joint family: taking their product introduces the forbidden rows (0,1) and (1,0). Both actual rows have x XOR y=0. Thus the nonempty MANY source fiber determines this readout uniquely as 0, despite not determining either bit individually.

3. The decoder on the reached image is uniquely h(A)=7 and h(B)=9. No source reaches Z, so h(Z) is not forced by Q=h composed with C. After Q(v)=8, two repair tags suffice and are necessary: the old A-fiber contains two distinct Q-values. For example, give u and w tag 0 and v tag 1; decode (A,0) to 7, (A,1) to 8, and (B,0) to 9. C already separates A from B, so tags can be reused between those fibers.

4. C answers every finite future observation: every action word is enabled and always produces observation 0. However, it has no exact summary update, because its A-fiber contains p and q whose successors have distinct summaries B and D. A constant representation K(x)=* is a correct coarser operational representation, with observation 0 and the total update * to *.

5. The shortest separating word is aaa, of length 3. Starting at x0 it succeeds at x3 with tagged answer SUCCESS(0); starting at x1 its third action is disabled, giving FAILURE. The empty word, a, and aa succeed with observation 0 from both sources. An absorbing failure state is a search device; adding it does not force a length-four bound for the original four-state partial machine. The handbook's source-machine bound is n-1=3, attained here. A generic five-state totalized bound of 4 would merely be looser.

6. The erased fourth bit is 0, since 1 XOR 0 XOR 1 XOR 0=0. An odd-parity reported word violates the admitted even-parity law. If exactly one position is wrong, flipping any one of the four positions restores even parity, so all four possible error locations remain compatible. Without a single-error premise, parity detects an odd number of bit changes relative to an admitted true word, which could be one or three; it does not locate them.

7. The center value is (2+4+6+8)/4=5. Throughout the square the interpolation weights are nonnegative and sum to one, so every value lies in [2,8] and is positive. These conclusions require the stated bilinear grammar. Let g be its interpolant for the observed corners. The continuous function f(x,y)=g(x,y)-6(1-x^2)(1-y^2) has the same corner values, indeed the same whole boundary, but f(0,0)=5-6=-1. Thus those observations alone force neither center value 5 nor positivity for arbitrary continuous functions.

8. A count alone is insufficient: equal-sized surviving families can contain different states and produce different later lane answers. Retain the compatible source family with source identities and joint prior constraints, or a representation proved sufficient for all requested continuations. The later observation map from the initial source is O composed with T, and must be applied to that same constrained source family. Four views generated from one source are related functions of it; their number supplies no statistical independence premise.

9. With M=7, mark multiples of 2,3,5,7 from each p^2 through M^2=49. Every composite k<=49 has a least prime factor p<=sqrt(k)<=7 with p^2<=k, so it is marked. In particular, 49 is marked starting at 7^2. No listed prime divides 47, and the factor bound excludes any other possible least prime factor; hence 47 is prime. This completed finite stage alone does not certify every later stage. The proved complete-M to complete-M^2 continuation rule, iterated by induction, justifies subsequent stages such as 49 to 2401 and unbounded finite continuation.

10. The stated bounded-side theorem concerns positive integers a,b,c and integer n>2, with min(a,b)<=4000. It imposes no independent cap on the other base, c, or n, and is stated as a theorem rather than an exhaustive test of every variable through 4000. The fixed-gap decision instead fixes integer n>=2 and positive integer gaps s,d, and completely decides positive integer a satisfying a^n+(a+s)^n=(a+s+d)^n. Decidability of each tuple does not imply that all tuples return NONE. At n=2,s=1,d=1, a=3 gives 3^2+4^2=5^2, so that aperture returns ONE(3). The handbook does not supply the long bounded-side proof, and it leaves the manuscript's independent unrestricted derivation OPEN.

11. No positive integer zero exists. Below 17 the normalized discrepancy is smaller than its negative value at 17; above 18 it is larger than its positive value at 18. The endpoints are nonzero, and no integer lies strictly between these adjacent integers. Positive normalization preserves the discrepancy's sign. Thus the monotonicity and bracket cover all positive integers for this aperture, yielding NONE. Other exponent and gap tuples require their own covered argument; this bracket supplies no uniform exclusion for them.

12. The partial decimals 0.3,0.33,... converge to the finite real number 1/3; infinitely many written digits do not make its value infinite. Repetition of a state in a deterministic machine with fixed dynamics repeats its subsequent trajectory and fixed readout, producing eventual periodicity. An unbounded integer sequence has no finite upper bound, although each term is finite; unboundedness alone need not mean monotone growth or divergence to positive infinity. A coordinate singularity is a failure or blowup of a coordinate description, which does not itself establish a physical singularity. None of these statements proves that a physical object reaches infinity. That claim needs a physical state map, evolution law, clock and corresponding physical interpretation or observable test.

13. Both a and b send total mass 2/3 into A and 1/3 into B; c stays in B. In block order (A,B), the exact reduced matrix is Q=((2/3,1/3),(0,1)). From a, the probability of B after two steps is 1-(2/3)^2=5/9 because B is absorbing. After the proposed b-row change, b sends mass 0 to B whereas a sends 1/3. Point-mass initial states at a and b have the same initial block but different next-block laws. Consequently no common reduced matrix on this partition works for every initial distribution, including the previous Q.

14. With the reduced initial distribution equal to the projection of the source initial distribution, the single-time marginal bound is min(1,t/50). It is 6/25=0.24 at t=12 and 1 at t=80, the latter being trivial. This argument does not itself bound every path statistic or infinite-horizon hitting-time statistic; those are additional receiver obligations requiring a separate argument.

15. The score ranges within the two fibers are 4 and 0, so the minimum worst absolute reconstruction error is 2. An attaining decoder returns -1 on the {-3,1} fiber and 4 on the {4,4} fiber. Any decoder value for the first fiber must be at least distance 2 from one of its endpoints, and its midpoint attains that lower bound. This is an exact result on the supplied finite scores, with no evidence of prediction accuracy on new data.

16. For chi=i/4, P_plus(0)=1/2 and P_plus(pi/2)=1/2+Re(i*i/4)=1/4. The complete compatible interval is p in [(2-sqrt(3))/4,(2+sqrt(3))/4], obtained from |chi|^2=1/16<=p(1-p). It has positive length, so there are MANY reduced density matrices, one for each p in that interval with the supplied chi. Even a unique reduced state would not identify every joint path-plus-marker preparation; that larger carrier contains additional marker and correlation information forgotten by reduction.

17. Before the operation both states have P_plus(theta)=1/2 for every phase, so the phase-only receiver cannot distinguish them. Under H, the p=0 state acquires chi=-1/2 and the p=1 state acquires chi=+1/2. At subsequent theta=0 their plus probabilities are respectively 0 and 1. Equal present phase summaries therefore do not ensure faithful continuation through H. The retained state must be refined to support that operation and its later observations; retaining p as well as chi supplies the full reduced path state.

18. The two joint probabilities of the corresponding marker outcome and plus are (1/2)(0.9)=9/20 and (1/2)(0.1)=1/20. Summing them gives the unconditioned plus probability 1/2. Unconditioned path coherence alone does not determine every selected distribution: averaging forgets how the patterns correlate with marker outcomes. The selected receiver needs additional joint path-marker information and the specified selection operation and weights, or equivalent complete conditional data.

19. Either single-bit query has worst compatible-hypothesis count 2: each answer 0 or 1 is permitted by both hypotheses. Both queries tie as one-step minimax choices, with no guaranteed immediate reduction. After first bit=1, the surviving joint rows are 11 for a and 10 for b. Thus second bit=1 selects a, while second bit=0 selects b. The two-step plan is useful precisely because retaining those joint rows allows the second answer to use the first constraint.

20. First, the losses concern different outcome carriers: a one-category prediction necessarily assigns its sole category probability one and obtains log loss zero without resolving the four outcomes. Freeze a common observable outcome space and scoring rule, either projecting both methods to the same coarse question or supplying a decoder to the finer outcomes and counting its information and cost. Second, omitting folds where selection failed changes the evaluated population and hides failures. Keep the frozen paired folds and report failed or OPEN selections, missing targets, coverage restrictions and the declared handling of such cases; OPEN selection provides no selected model to refit. A valid comparison must also freeze candidate and baseline definitions, selection rules, holdout population/splits, available input information, cost budget and rejecting outcome before examining results, and report data/model availability, execution status and actual results at their evidence grade. A correct finite preservation theorem concerns its declared model and receiver; it repairs neither mismatched prediction targets nor selective omission of failed evaluations, and does not establish empirical generalization.

