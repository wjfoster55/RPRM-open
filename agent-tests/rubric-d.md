# Frozen scoring rubric D

Twenty questions, four points each, total 80. Award one point per numbered
criterion within the corresponding line. Exact equivalent formulations
receive full credit. Require the requested reasoning, not only the final
number. An arithmetic slip loses its calculation point rather than every
conceptual point. A missing answer receives zero. Record every deduction
and do not change criteria after seeing answers.

1. (-2,1),(-1,0),(0,-1),(1,-2) complete; uses admitted integers/no wrap;
   fully supplied lawful row has {()}; distinguishes one empty completion
   from no completion.
2. Product adds 01 and 10; joint source not preserved; actual XOR always 0;
   multiple sources can settle one readout.
3. h(A)=7,h(B)=9; unused Z unconstrained if extension even admitted;
   revised Q needs at least two tags inside A; two tags attained and reused
   across old fibers with decoding by (C,tag).
4. Every future answer OK(0); C update at A conflicts B/D; gives explicit
   successor counterexample; constant representation is a correct quotient.
5. aaa shortest; x0 succeeds OK(0); x1 fails; failure also gets a separate
   initial block, so four source states admit n-1=3 bound, not forced 4.
6. Erasure 0; odd syndrome detects under single-error assumption; wrong
   position not localized; identifies more than one compatible error position
   or explicitly notes no locator information.
7. Center 5; positive via nonnegative weights summing to one; arbitrary
   continuous grammar not covered; concrete bump f-M(1-x²)(1-y²), M>5,
   preserves corners/boundary and makes center negative (or equivalent).
8. Count loses identity needed for future constraints; retains full joint
   survivors or complete reopen route; maps later observation through T;
   common-source generated views do not gain independent measurement status.
9. Next bound49, primes2,3,5,7 multiples startingp²; 49 marked by7;
   47 unmarked and factor-bound argument certifies; all-stage result needs
   the proved repeatable continuation, not unaided finite observation.
10. Positive roots, integern>2,min≤4000; no additional independent larger
    root/exponent caps; fixedgapn≥2,s,dpositiveinteger per-tuple decision;
    n2,s1,d1 givesONE3, not universalNONE.
11. Normalized signs have same sign as discrepancy for positive a;
    monotonicity excludes below17 and above18; no integer strictly between
    17and18; only the supplied aperture, other tuples need further coverage.
12. Repeating decimal finite limit; repeated state has future recurrence;
    unbounded sequence not a finite numerical value and coordinate singularity
    depends on coordinates; none alone proves physical infinity.
13. Q[[2/3,1/3],[0,1]]; step-twoB=5/9; originalsameaggregateArows/
    pointinitialargument; changedbnextB0versusa1/3invalidates all-initialquotient.
14. 12/50=6/25; min1,80/50=1; matched initial projected distribution;
    marginal bound does not establish every path/infinite-horizon statistic.
15. Worsterror2; decoderfirstfiber-1,second4; extremes lowerbound and
    midpointattains; finite audit does not prove held-out prediction.
16. Pplus0=1/2; Ppluspi/2=1/4; p∈[(2-sqrt3)/4,(2+sqrt3)/4] henceMANY;
    reduced-stateuniquenesswouldnotidentifyarbitraryjointpreparation.
17. Bothflatphase1/2before; p0→chi-1/2; p1→chi+1/2;
    phase-onlysummarycannotupdatethroughHwithoutretainingmore.
18. Jointplus0.45and0.05; unconditioned0.5; alloutcomeweightsrequired;
    marginalcoherencealoneinsufficientforselecteddistribution/jointor
    outcome-resolvedstateisadditionalinput.
19. Eitherfirstqueryworstcount2; first1second1selectsa;
    first1second0selectsb; noimmediateshrinkdoesnotruleoutusefultwostepplan.
20. Incomparableoutcomecarriers; requirecommonfrozenobservableorchargeddecoder;
    OPENfoldsmustremainfailuresandpairedpopulation/coverageexplicit;
    correctpreservationtheoremdoesnotrepairunmatchedempiricalevaluation.

Interpretation: This is a frozen finite comprehension assessment. A score is
evidence about the supplied handbook version and answering context only.
It is not a universal agent guarantee or a test of the hoped-for scientific
outcomes. Success requires correct complete answers on all twenty questions;
retain any failures and repair the handbook or assessment only with a new
explicit version and a fresh holdout where the change affects comprehension.
