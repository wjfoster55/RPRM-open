# Assessment B: answer key

Apply the [scoring rules](rubrics.md). Each numbered item is worth two points.

1. At `L=3,u=0`, the relation is `x=3`, independent of R. With `x=3`, every real R is admitted: a complete MANY family. With `x=4`, the fiber is NONE. Dividing by the zero coefficient would lose the actual branch.
2. Exact image equality transfers existence both ways. An abstract singleton does not imply a singleton source fiber or reconstruct which of a,b was supplied. Injectivity on the relevant source fiber, together with the image equality, would make the restricted map a bijection and transfer uniqueness and reconstruction.
3. Repair the implementation's receiver types and equality: `Bool(true)` and `Int(1)` are distinct. A summary may merge source states only when Q is equal under that declared equality. No particular source-state merge is given, so refinement is conditional on such a merge actually occurring.
4. The complete compatible-hypothesis fiber is NONE. Vacuous agreement does not determine a factual readout about an actual source. Check the observations, inference, and model/source coverage; that unsupported readout remains open.
5. `ab` is a shortest observation witness: x reaches t with output 1, while y reaches s with output 0. The empty word and any one-letter word return equal observations. If r and s have distinct initial summaries, action a already violates successor congruence of the x,y merge. If they initially share a summary, action b first separates them, and then a separates x,y. Repairing one requested observation need not establish all operational congruence conditions.
6. Both lists have successor set `{A,B}`, so they agree for this step under set-valued semantics and equal current observations. They disagree as multisets. Lists do not supply probabilities automatically; uniform selection over occurrences gives A-masses `2/3` and `1/2`, while another declared weighting rule could differ.
7. Neither binary truth value fits an at-most-one-error guarantee: either value makes two reports wrong. The observed row is inconsistent with the supplied model and has no lawful completion. Do not invent a majority or identify a liar.
8. Merging the programs is correct for the endpoint partial-function/failure receiver. It fails for step count or a richer trace receiver. Retain the count when that is the question, or the requested ordered trace when full trace information is required.
9. The tag is sufficient for the parity question but supplies no end-to-end speedup when its construction already performs the expensive computation. Compare against an ordinary baseline under a frozen workload and cost model, charging access, preprocessing, queries, and storage; report one-query and declared repeated-query costs without free preprocessing.
10. The complete family is `{(t+7,t):t∈Z}`. Every such pair solves the equation; every solution is in the family by setting `t=y`. This proves the particular fiber, can serve as its certificate, and can induce an enumeration. It does not prove a general integer-equation solving procedure.
11. The result is finite implementation agreement on the supplied synthetic rows. A proposed external test must specify an observable target, freeze the bridge/predictions, and evaluate held-out observations against a baseline with a failure criterion. A leakage or label-permutation control can challenge apparent success. Internal labels do not establish or diagnose personal beliefs or traits.
12. A sound summary explains the unifying aim through explicit carriers, relations, questions, and preserved structure; supplies a valid scoped bridge such as injective relational presentation with image-guarded first-order translation; gives a concrete exposed question or lost distinction; relates the work to existing mathematics without name-based novelty; and keeps physical interpretations dependent on separate evidence. The requested five-sentence form should remain connected prose rather than unsupported claims of universality.

Question 5's initial-partition wording is genuinely ambiguous. The recorded response recognized both readings and gave the correct invariant shortest witness. Future revisions should name the initial partition blocks explicitly; changing that wording creates a new question-file version.
