# Assessment C: answer key

Apply the [C scoring rule](rubrics.md): one point for the correct concrete result and one for its exact scope and premises, per question.

1. Modulo six, the complete fiber is `{(0,2),(1,1),(2,0),(3,5),(4,4),(5,3)}`. On `{0,1,2}` with ordinary addition it is only `{(0,2),(1,1),(2,0)}`. Both are MANY under their own carrier and law. The latter marginal product adds six false pairs: `(0,0),(0,1),(1,0),(1,2),(2,1),(2,2)`.
2. The integer preimages are NONE for −1, MANY `{−2,2}` for 4, and ONE(0) for 0. The converse relation is neither a total nor a single-valued inverse on the integers. A genuine square-root inverse requires suitable source and target restrictions; another carrier changes the question.
3. Endpoint projection preserves endpoint values and reachability, and any question constant on endpoint fibers. It can forget the middle witness and multiplicity. A complete source consisting of the two displayed paths has exactly two preimages; if they are merely examples, only at least two are established. Recovering all compatible middles requires the complete retained relation or a reopen route; recovering the originally selected middle additionally requires a distinguishing witness. A unique endpoint pair does not imply a unique path.
4. Q does not descend because 0 and 2 have the same parity and different Q-values. The least question refinement is `(C,Q)`, with fibers `{0}`, `{2,4}`, and `{1,3,5}`. Two reusable tags suffice and are necessary in the even fiber; for empty X the minimum tag carrier has size zero. This is an information bound, not a construction/access/runtime bound.
5. The three future classes are singletons. Word `a` is shortest for s,t; the empty word separates t,u. Present observation merges s,t despite successors having different observation summaries, so it is not an exact operational quotient.
6. Neither an infinite unfinished action enumeration nor undecidable observation equality satisfies the finite-algorithm contract. Require finite state and action sets supplied as complete tables, deterministic partial transitions with exact enabledness, and exactly decidable observation/transition comparisons. Shortlex search also uses the declared finite action order. Finite state count alone does not make scanning all actions terminate.
7. A failed execution and a successful execution returning the ordinary value named NONE need disjoint tags. Enabledness differs, so the proposed p/q merge fails the operational quotient even when current observations agree.
8. The successor-class sets agree, so the local set-valued check passes under equal current observations. The stochastic class masses differ, so its law-preserving merge fails. Support does not determine the probability law; other actions and richer receivers still require their own checks.
9. The source universal statement is true, the unguarded target statement false, and the image-guarded target statement true. Restrict quantifiers to the reached image. Faithful translation preserves truth under the stated hypotheses; it does not decide arbitrary first-order truth.
10. The erased bit is 1. With exactly one error in received `0111`, the four possible originals are `1111`, `0011`, `0101`, and `0110`. Each flips one position and has even parity; these exhaust the positions. Unknown corruption gives no unique original. Three agreeing reports do not establish truth without a suitable error guarantee or separate source evidence.
11. The specified integer is `10^(10^12)`, its decimal length is `10^12+1`, and its last digit is 0. Under zero-based indexing, the final position is `10^12`; valid positions and the representation/cost of bounds and indices must be declared. Constructor-based queries can avoid expansion without implying constant bit cost or geometric dimension. Prime exponent 11 gives the composite `2047=23·89`.
12. The direct conclusion is agreement on the complete supplied finite case set, not even correctness of both outputs unless independently established. Self-acceptance or mutual agreement cannot circularly prove general soundness. A biological use needs an explicit model-to-system bridge and evidence. Retain the exact carrier, laws, receiver, versions/source or generator, finite coverage, observations and failures, unproved obligations, and a usable recovery route with concrete continuation tests.

Question 3 does not say that the two exhibited paths exhaust the source. The recorded response correctly handles both readings. Keep this qualification when grading the historical question; specify exhaustiveness explicitly in a future question version if a single complete fiber is intended.
