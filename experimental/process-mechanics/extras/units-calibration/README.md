# Units and calibration

Synthetic measurement toy: translation preserves an OLS slope; changing the
ruler (a.u. vs pixels) changes the scientific question. Equal numeric arrays
with different units are not the same query.

Evidence grade: `NEW_ILLUSTRATIVE_DEMO`. Not a second Liang extraction.

```sh
python -I -B experimental/process-mechanics/extras/units-calibration/example.py
python -I -B experimental/process-mechanics/extras/units-calibration/check.py
```
