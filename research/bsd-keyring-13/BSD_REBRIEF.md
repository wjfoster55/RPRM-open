# Keyring 13 — concise BSD rebrief

New proposed model: three component hoops carrying integer, middle and
joint-tail records, threaded by a fourth common hoop. This supersedes
using the preceding two-arc picture for the current user hypothesis.

Proved for the explicit embedding: four disjoint components, three
unit-magnitude connector links, and a satellite unlink. The numerical
labels have not been shown to generate this topology.

Fresh exact checks: both existing distance-interval records read back
from their three blocks; all 5,050 declared two-digit interval records
recover exactly; 200 one-carry states preserve value and tagged inverse;
2,100 joint interval boundary cases identify when a shared middle prefix
must split or move to an extended offset chart. Browser rotation and
desktop/mobile fit were checked separately.

Retained boundary: the shape alone admits all 5,050 different interval
labels in the finite control family. Its topological linkage does not
select a scalar multiplier. Carry is an available exact positional
operation, not a proof of an analytic-to-arithmetic identity.

No new height refinement, Sha result, BSD coefficient identity, or general
BSD theorem is claimed. Earlier rank/basis/local and 2-/5-primary Sha
dependencies remain unchanged. The missing operational BSD comparison
and total Sha determination remain OPEN.
