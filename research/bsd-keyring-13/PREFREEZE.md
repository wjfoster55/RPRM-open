# Keyring 13: three loops and a fourth connector

User correction: the proposal is three component hoops threaded by a
fourth hoop, like a keyring. The assigned roles are integer block `2.`,
middle block `0340`, and the joint pair of tail blocks. The visible line
carrying them is proposed as part of the connector loop. Treat this as a
new candidate topology; the earlier two-arc model is not imposed on it.

K1. Construct four disjoint Euclidean circles: a radius-3 horizontal
connector and three radius-3/4 vertical radial circles centered on it at
angles 0, 2pi/3, 4pi/3. Check connector-to-satellite linking and separation.
The drawing is a proposed representation, not a derivation from digits.

K2. Give the connector an explicit positional readback law for one joint
interval record (integer, middle word, paired tail words, widths). Replay
both existing depth-9 and depth-10 interval readouts exactly. The numerical
bounds are reused as attributed inputs; do not rerun height arithmetic.

K3. On the complete finite carrier of two-digit ordered tails 0<=a<=b<100,
hold integer 2 and middle word 0340 fixed. Test whether the same keyring
topology distinguishes the 5,050 resulting interval records. Retaining
numeric blocks and their widths must give an exact inverse.

K4. Test one-carry boundary transfers from tail to middle and middle to
integer with exact rational evaluation. The finite one-carry input range
is explicit; carry state is retained. This proves a positional transfer
law, not a BSD analytic-to-arithmetic comparison.

Keep the actual analytic coefficient, period, height regulator and Sha
readout separate. User-specific loop operations and their BSD bridge are
OPEN. Write only this new experiment and its visualization; old packets
and other active lanes stay unchanged.
