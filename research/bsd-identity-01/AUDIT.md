# Independent reviews, fresh runs and retained limits

The main task and three bounded subagents worked in this new experiment
directory. Mathematical deductions, primary-source applications, exact
finite computation and open hypotheses were kept separate.

## Input freshness

`input_source/work/` contains unchanged copies of the preceding rank-two
source and frozen numerical budget. No old evidence was copied in.
The new aggregate executed all seven stages into a fresh nested run.
Those stages reproduce the curve-specific descent, full basis, local
data, infinite-series interval, interval audit, midpoint identities and
period/regulator comparison. The current root then reconstructed the
quotient from exact Gram, period and analytic endpoints for the new gate.

The earlier `BSD_RANK_TWO_01_RETURN.zip` was read and its hash remained
`00acf0d413f4cab404af38ebf0f48ee926e8aeb39377710a11fd97eb0647ce0e`.
No changes to that delivered archive are part of this continuation.

## Exactness and readout review

A separate agent independently reassembled the exact quotient,
verified source and receipt hashes, and checked all denominators through
10775. The largest interval-isolation bound is 10774; the first
alternative above 1 is 10776/10775. The first denominator giving an
alternative below 1 is 12586. The proof only transfers to the actual
quotient if bounded rationality is independently supplied.

The whole-unit/remainder transport was checked at the same known scale.
For rational inputs its complete observation fibers include intersection
with Q. The all-level argument concerns one fixed value. The signed BSD
discrepancy is handled by observing its nonnegative magnitude. A new
finite congruence control retains all five residues above coarse zero
modulo 25 and leaves the actual trace null.

## Height and analytic comparison review

The root independently inspected the projective duplication, primewise
gcd bounds, torsion exceptions, absolute convergence and logarithmic
telescoping. The determinant error bound follows by bounding the two
diagonal errors by e and the off-diagonal error by 3e/2; the quadratic
error constant is 1+9/4=13/4. The exact increment retains both mixed
products and the square term.

The proposed matching certificate explicitly requires an initial
boundary, an exact law for every stage, and a terminal bound tending
to zero. Defining the potential to be the discrepancy supplies no
independent proof of that terminal bound. No matching potential was
constructed for E34.

## Odd-prime review

Two agents and the root inspected the relevant primary equations.
The independent reviewer confirmed good ordinary non-anomalous p=5,
the point count 8, the CM-compatible isomorphism and isogeny, the least
real period factor 2, Gaussian unit count 2048, degree 256, derivative
polynomial 24ρ⁵+3468ρ, and the corrected critical-value factor.
The negative-D model −289 is within the stated trace lemma's domain;
no odd-exponent restriction from a different lemma was imported.

The direct even-D conductor discrepancy is reported and not used to
admit that formula. The odd-D isogenous model supplies conductor 68.
The historical factor-four correction in CLS II is retained. Least
positive real period and the full real integral are different conventions
and are not interchanged.

An intermediate review saw search count three while the fourth search
was being completed. The final source, receipt and note agree on four
queries in three tool calls. Their hashes match. This timing issue did
not alter the mathematical reduction. The critical trace, its valuation,
and Sha[5^∞] conclusion remain uncomputed/NOT_ESTABLISHED.

## Theorem review and limits

The root directly checked the published Burns–Kurihara–Sano theorem's
stated conjectural hypotheses and the exact comparison in Proposition
4.14. The six-search review distinguishes central from noncentral
values, real from p-adic regulators, number fields from function fields,
and Q-rank two from a simple Hecke zero after CM base change.

The literature search is bounded; it does not prove no other theorem
exists. The computations do not prove general soundness of their own
implementations or formally verify the written deductions. The final
aggregate records process completion separately from mathematical OPEN.
