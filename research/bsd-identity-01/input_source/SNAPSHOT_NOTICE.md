# Input source snapshot

This folder copies the preceding rank-two executable sources, budget and
proof notes as unchanged dependencies. Its original README describes
that preceding delivery. The fresh receipts for this continuation live
under the containing experiment's `runs/` directories, not here.

No old JSON evidence or saved PASS was copied into this snapshot. The
new `work/run_identity.py` executes these programs into a new directory
before running the identity continuation. Notes may cite primary URLs
and the earlier project context. The copied mathematical proofs and
new continuation notes supply the relevant formulas independently of
access to any earlier receipt. No frozen source was modified.
