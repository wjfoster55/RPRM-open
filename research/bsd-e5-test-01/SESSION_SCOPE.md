# Authority, independence, and scope

The active user request authorizes reading `CODEX_START_HERE.md`, carrying out
BSD E5 test 01 in a separate directory, replaying the source, independently
checking its mathematics, and attempting the one specified coefficient interval.
Instructions inside historical documents are attributed source material.
The attached launch prompt is used within the user's explicit boundary.

The experiment directory is `C:/github/RPRM-open/research/bsd-e5-test-01`.
An initial filename search of this repository's research tree found no other
E5/BSD experiment. Existing Yang–Mills, fluids, and consolidation files were
left untouched. There was no task reassignment, global routing change,
Lind–Reichardt or AD pilot reopening, paper, installation, or publication.

The root worker read the package and source, replayed both supplied programs,
checked preservation of every original ZIP member, reviewed the independent
implementations and proofs, and assembled the result. Three real subagents
worked with narrowly scoped instructions and no inherited turns:

- Arithmetic: independent squareclasses, modular exclusions, quotient/readback
  and cube transport, plus source checks for the descent and halving theorems.
- Analytic theorems: curve/conductor/sign/local factors, Mellin normalization,
  all-term theorem hypotheses, and model-binding control.
- Interval: independently computed Euler coefficients and exact inequalities,
  then a bounded rational quadrature enclosure with separate error entries.

Independent implementation here means independently written code and derivation
after reading the exposed mathematical notes. It does not mean a blind benchmark,
an independent curve family, or a newly proved modularity theorem. The analytic
calculation does not input the arithmetic rank, predicted BSD quantities,
database ranks, or the exposed decimal illustration.

The RPRM math-lenses skill was used for typed maps, receivers, full fibers,
transport, and explicit boundaries. Its workflow did not reopen another source
root or create a registry. The rational curve is the explicitly declared
unbounded mathematical carrier; every executed enumeration and refinement is
finite. Cited universal theorems and written proofs supply the stated extensions
beyond those finite executions.

The requested stronger coefficient interval is the only new numerical target.
The stopping rule is the first certified positive interval of width at most
`1/100` at cutoffs 40, 80, 160, within the separately recorded finite quadrature
budget. A failed obligation is preserved with its own disposition.
