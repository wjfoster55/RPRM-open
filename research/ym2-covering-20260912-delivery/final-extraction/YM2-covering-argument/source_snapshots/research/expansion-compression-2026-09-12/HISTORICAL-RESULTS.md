# Historical expansion, compression, and retained remainder

Recovered 12 September 2026. This note records existing results and their corrections for the current expansion/compression investigation. **The useful established pattern is selective exposure: retain a reconstructible source or complete fiber, operate through the sufficient current coordinates, and reopen the particular remainder that a changed operation or question needs.** Several exact donors already implement parts of that pattern. Their composition into a general adaptive controller is a further task.

Evidence labels below distinguish **original user wording**, **historical written derivation**, **recorded finite PASS**, and **current synthesis**. No historical campaign was rerun here. Source reports and result files were read directly; their PASS labels are attributed recorded evidence, not fresh execution receipts. Historical Quantum Research and Alpha files were read-only, and no Memory Fabric compilation, ingestion, or reindexing was performed by this subtask.

## Source entrance and historical intention

The recovery entrances are [Zero and rails](C:/github/RPRM-open/recovered-concepts/ZERO-AND-RAILS.md), [Prestige and number operations](C:/github/RPRM-open/recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md), and [the prior recovery](C:/github/RPRM-open/recovered-concepts/RPRM-CONCEPT-RECOVERY.md). The current mathematical domain note is [Operational Layer Foundation](C:/github/RPRM-foam-development/research/RPRM_OPERATIONAL_LAYER_FOUNDATION_AND_PRIME_PROGRAM_2026-08-28.md). The current Foam checkout and the historical OneDrive roots are different evidence roots.

The retained user corrections give the intended problem more substance than a metaphor:

- The half/offset exchange asks about two neighboring gaps plus their direct outer relation; its next exchange asks to track the folds and operations while climbing a hierarchy. The original half turn is at [native session line 28540](C:/Users/bkbee/.codex/sessions/2026/08/10/rollout-2026-08-10T09-31-01-019fec4c-73db-74c2-9079-55df4b51ab6c.jsonl:28540), timestamp `2026-08-12T01:21:01.410Z`; the coherent exported sequence is retained in [Zero and rails](C:/github/RPRM-open/recovered-concepts/ZERO-AND-RAILS.md:85), with original export span 12327–12459.
- Prestige starts as a completed lower course usable as a next-level one. The immediate **pocket-pi correction** changes “contains the whole payload” to “retains its relationship/outline and a route into it.” See [U10](C:/github/RPRM-open/recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md:66), native USER `2026-08-26T19:41:43.971Z`, source session line 7150.
- **Dependency distance** concerns the lower work required before an outer move, not numerical magnitude. See [U15–U16](C:/github/RPRM-open/recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md:96). **Same landing does not erase the construction route**, including two prestige ones used as a prestige two versus constructing the latter by its original route; see [U25](C:/github/RPRM-open/recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md:120).
- Later user wording explicitly proposes segmenting complexity, trying approximate stages, and allowing complexity to decrease through a release while some attained structure remains. These are source proposals, not an already proved entropy or complexity law; see [U34–U35](C:/github/RPRM-open/recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md:132).

The source corrections therefore support a dynamic research question: which detail must remain active, which can be retained through a lower access route, and when must the latter be reopened?

## 1. Stacking binary coordinates does not add their values

**Historical written derivation.** The [centered-half scout, §§2–6](<C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/Quantum Research/RPRM_CENTERED_HALF_FILLING_ZERO_SCOUT_0_1_SHADOW.md>) specifies the exact product maps

```text
{0,1}^d <-> {-1/2,+1/2}^d <-> {-1,+1}^d,
u = x - (1/2,...,1/2),  s = 2x-1.
```

For example, `(-1/2,-1/2)` contains two distinct coordinate occurrences. It is neither the scalar `-1` nor two applications of a negation. Projection or summation is an additional map with its own fibers. The centering maps commute with coordinate permutations and products; they do not supply an unprovided operation between dimensions.

On the different rational pair carrier `S+R=1`, `z=S-R` has inverse `S=(z+1)/2, R=(1-z)/2`. Thus the balanced pair `(1/2,1/2)` has zero readout. The endpoint-only bit carrier `{(1,0),(0,1)}` excludes that balanced pair. Cubical `FREE_SPAN`, scalar zero, vacancy, and missing input are still different typed objects. In particular, centering the symbol `*` labels the full spanning face, not just its midpoint point.

This matters to compression: duplicate displays may encode one shared state, independent stacked axes encode a product, and correlated lanes encode a constrained subset. These have different fibers before any compression is chosen.

## 2. Common and relative modes already form an exact reusable split

**Historical written derivation.** For `n>0` and exact rational `x in Q^n`, the same [scout, §§3–4](<C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/Quantum Research/RPRM_CENTERED_HALF_FILLING_ZERO_SCOUT_0_1_SHADOW.md>) gives

```text
t = sum(x_i),  u = x - (t/n)1,  sum(u_i)=0,
x = (t/n)1 + u.
```

At fixed unit total the common lane is `1/n`, not universally `1/2`. For a pair of positions, `c=(A+B)/2` and `r=(B-A)/2` reconstruct `A=c-r, B=c+r`. Under a supplied affine map `f(p)=Mp+b`, the pair becomes `(Mc+b, Mr)`: translation acts on the common position and cancels from the relative direction. These formulas require a scalar domain permitting division by `n` or two. Arity zero is a separate nullary occurrence, with no invented mean.

The scout's proposed binary-stack/handoff implementation was still prefreeze. The separate [finite zero-balance shadow](<C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/Quantum Research/experiments/RPRM_FINITE_ZERO_BALANCE_SHADOW_01/README.md>) records exact lift–swap–land checks only on its three-state and five-state zero-sum pair carriers. Its own hostile case shows that finite conservation does not imply zero balance. Neither record establishes a general compression speedup.

## 3. Fold lineage gives a scale coordinate and a reopen route

**Historical written derivation, not a new bench.** The [fold-lineage scout](<C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/Quantum Research/RPRM_FOLD_LINEAGE_DYADIC_ADDRESS_AND_LOCALIZATION_SCOUT_0_1_SHADOW.md>) defines rooted dyadic words `w=b_1...b_n` and intervals

```text
k(w)=sum_j b_j*2^(n-j),
I_w=[k(w)/2^n,(k(w)+1)/2^n].
```

Appending a bit selects a child. Popping a bit is a two-to-one map unless that bit or the original lineage is retained. Depth determines scale; it does not determine location. Words `01` and `10` have equal bit counts and different intervals. In several fixed independent axes, ordered words may be kept separately per axis; same-axis bit order still matters.

Counts can replace ordered operation words for an endpoint receiver only under proved relations, such as a single involution or commuting generators. Noncommuting operations, state-dependent enabledness, or a trace/cost receiver require more. A local neighborhood determines available moves, but a root/chart/sheet anchor is generally needed to locate which repeated copy is occupied.

The scout also separates dilation, branching, dual rails, covers, products, overlays, and folding quotients. Thus “two-by-two on two-by-two” can mean four footprints, eight labeled sheet positions, or sixteen refined/joint cells. There is no untyped doubling law that chooses among them.

## 4. A movable cut and an early carry share exact retained arithmetic

**Historical identities plus recorded finite PASS.** [Audit105](C:/github/RPRM-foam-development/experiments/RPRM_RELATION_RESIDUAL_LENS_LOCK_CUT_ROTATION_AUDIT_105/REPORT.md) treats ordered tuples over `Z/10Z`. Anchor `x_0` plus residues `x_i-x_0 mod10` is invertible; dropping the anchor leaves `MANY(10)` for every nonempty admitted arity. Holding the residues fixed and translating the anchor preserves the declared relation shape. Ten cut charts `rep_c(p)=c+((p-c) mod10)`, `c=-9,...,0`, transport the operations by conjugation.

The precise offset distinction is

```text
+0.4 = -0.6  in R/Z,
4 = 10*0+4 = 10*1-6  on the integer lift.
```

The cover difference is one unit; the quotient difference is zero. Separately, the unsigned pair `(0.4,0.6)` has total one and contrast `-0.2`. These are separate receiver choices. Merely repeating either offset on several axes does not invoke a sum.

Audit105 records 111,100 relocalizations, 10,000 three-chart compositions, 7,000 transported-operation cases, and seven hostile controls. Naive addition to the bounded display fails in 1,200 seam cases. Its recorded result root is `6e72093623603eb2fe3fd95ff035b1b3252fc86b94cd5a36bfedbd552c10d96b`.

**Recorded finite PASS with a named selector.** [Audit116](C:/github/RPRM-foam-development/experiments/RPRM_ANTICIPATORY_SIGNED_CARRY_TENSION_AUDIT_116/REPORT.md) supplies radix `b>=2`, value `V_b(q,d)=bq+d`, and early promotion

```text
A_b(q,d)=(q+1,d-b),
bq+d=b(q+1)+(d-b).
```

Retained radix, chart/direction, parent, and signed residual permit the inverse `(q',r)->(q'-1,r+b)`. The negative residual is exactly minus the remaining margin `m=b-d`. At the hard seam `d=b`, the residual is zero. On the declared two-chart carrier, forgetting the chart yields `MANY(2)` value representations.

For the particular local cost `tau_wait=d`, `tau_carry=b-d`, waiting wins below half, early carry wins above half, and even-base half gives `MANY(2)` until a tie policy is supplied. Thus `4=10*1-6` is an exact chart move but does **not** win this cost; `8=10*1-2` does. The speculative two-thirds threshold is not universal.

The recorded nested census covers radices 2–10 and widths 1–4: 28,794 source words and 57,588 reconstructions under both half-seam policies, preserving `N=sum_i digit_i*b^i+terminal_parent*b^w`. The report records 12/12 hostile controls, 16/16 unit tests, and result root `9b3991d641038559e669eb899f79fd93749a45382dd13fcea436765651ff5f02`. This is a direct donor for provisional promotion with retained debt, not a physical tension law or globally optimal carry policy.

## 5. Expand directions, cancel a named residual, then fold

**Historical derivation plus recorded finite PASS.** [Audit89](C:/github/RPRM-foam-development/experiments/RPRM_PRIMITIVE_LATTICE_DIRECTION_SHELL_AUDIT_89/REPORT.md) preserves a qualified version of expansion turning a `1–2` move into `1–1`. For `U=(1,2), V=(2,1)`, a word with counts `(a,b)` has

```text
E=(a+2b,2a+b),  E_y-E_x=a-b.
```

Equal counts cancel the residual and the endpoint gcd-normalizes to `(1,1)`. Repeating only `U` never changes its ray. Primitive direction shells expand while retaining every earlier ray; at radius 32 the recorded census has 2,592 oriented rays. All 65,536 length-16 words fold to 17 endpoints; the diagonal cell contains 12,870 histories. `UV` and `VU` have the same endpoint and different intermediate vertices.

The compact symbolic example preserves residual one after an unpaired `U` even when the relative error is tiny. Approximate diagonal alignment does not become exact balance. The reusable result is that expansion can admit a complementary route and compression can retain the balance readout; it need not preserve the strong trajectory.

## 6. Prestige can share a closed object without unfolding it

**Conditional construction with recorded finite PASS.** [Round16 protocol](C:/github/RPRM-foam-development/experiments/RPRM_CTHULHU_ZERO_ROOT_ONE_REENTRY_AUDIT_16/PROTOCOL.md) and [result](C:/github/RPRM-foam-development/experiments/RPRM_CTHULHU_ZERO_ROOT_ONE_REENTRY_AUDIT_16/RESULTS.json) preserve one supplied root, a returned-self `NN` path, separately supplied `RELATION` and `CLOSE`, then promotion. Visible roots remain one while their retained lower artifacts and histories differ.

The recorded grade-64 construction has 321 DAG nodes (`1+5n`), versus naive unfolded size `6*2^n-5`; it has no value, sharing, or reopen failures. This is evidence for sharing repeated dependent structure through a retained access route. It does not show that every problem admits that DAG, that all arithmetic became constant cost, or that `RELATION`/`CLOSE` were authored by the one.

The narrower [Audit111 path construction](C:/github/RPRM-foam-development/experiments/RPRM_FIXED_PATH_KERNEL_COMPLETE_SEAM_FIBER_EXECUTED_ORACLE_AUDIT_111/REPORT.md) proves by structural induction that `ROOT ATTACH^(n-1) CAP PROMOTE` generates the canonical rooted path for every supplied finite `n`. `DETACH`, `UNCAP`, and `REOPEN` invert the generated image. Removing the promoted child's receipt loses child-distinguishing futures. The target and promotion/termination rule remain supplied; the earlier rejected Audits109–110 do not inherit Audit111's repaired result.

## 7. An existing operation extension reopens only the required remainder

**Historical written derivation plus recorded finite PASS.** [Audit99](C:/github/RPRM-foam-development/experiments/RPRM_VERSIONED_OPERATOR_REOPEN_BIG_PEEK_AUDIT_99/REPORT.md) is the most direct existing dynamic-compression donor.

In its additive panel, `h in Z/MZ`, an admitted nonempty action set `K` has `g=gcd(M,K)` and `h=gq+r`, `0<=r<g`. The all-word executable quotient is `q`. Add one allowed action `k` and define

```text
g'=gcd(g,k),  d=g/g',
r=g's+r',
q'=dq+s=floor(h/g').
```

Exactly the migration tag `s` must move out of the formerly cold residue. `(q,s)<->q'` and `(s,r')<->r` are invertible. The new quotient splits each old cell into `d` cells with `g'` strong states each. A redundant action has `d=1`; a strict extension has reverse version fiber `MANY(d)`, reopened by `s`. Different action orders can have different route digits while yielding the same final quotient, direct tag, and cold remainder.

Its separate mixed panel has `h=gq+r in Z/(3g)Z`, `A(h)=h+g`, and new `T(h)=2gh mod3g`. The exact minimal all-finite-word receiver is `(q,s)` where `s=r mod3`, with

```text
A(q,s)=(q+1 mod3,s),
T(q,s)=(2*((g mod3)q+s) mod3,0).
```

Only the ternary shadow reopens; `t=floor(r/3)` stays cold and reconstructs `h=gq+3t+s`. Different `q` is visible now; different `s` at equal `q` is separated by one `T`; equal `(q,s)` stays equal under both actions. This proves the stated minimality. Its controller terminates at one visible endpoint while retaining nine distinct complete traces, so terminal equality cannot replace executable state.

The additive census records 458,633 extension edges for `M=2..16` and 1,506,767 two-action diamonds. The mixed census covers `g=3..512`, with independent Moore refinement for `g=3..64`. Two producer passes and a separate package-free cold replay record PASS, 22 killed mutations, and result root `cfb61e49f9493413f91b219091e4a1cbd60230936a63d80f6ee92ef4589c99e0`. The report explicitly leaves arbitrary new operators, general affine programs, live lower-block carries, and performance advantage outside its result.

## Current synthesis and remaining seams

The [existing operationalization law, §5](C:/github/RPRM-foam-development/research/RPRM_OPERATIONAL_LAYER_FOUNDATION_AND_PRIME_PROGRAM_2026-08-28.md:408) already writes a strong split `phi:X<->H×K` with `phi T phi^-1(h,k)=(F_k(h),k)`. It requires invariant locked data, retained branch inputs, legal compressed operations, admitted landing, evaluable readouts, and commuting direct execution. Audit99 supplies a concrete version upgrade when a future needs part of `K`; Audit116 supplies a concrete signed debt when a parent is promoted early; Round16 supplies shared child access instead of naive expansion. Their current composition is a **research synthesis**, not a newly earned cross-domain theorem or controller benchmark.

The following corrections remain load-bearing:

- **Double negative:** the exact supplied-involution donor returns `U -> N(U) -> N²(U)` to the same weak value while retaining the route. It does not make two negative coordinate occurrences into a composition. The [oriented-zero source note](C:/github/RPRM-foam-development/experiments/RPRM_CTHULHU_ZERO_ROOT_ONE_REENTRY_AUDIT_16/POST_FREEZE_NEGATIVE_ZERO_SOURCE.md) is still a post-freeze candidate: `Z^-=(weak 0, negative orientation, root/history)`. Scalar `-0=0`; a second incidence and a separate closure/event receiver would have to earn a first one. Empty-path versus distinct two-incidence closure remains OPEN.
- **No single recovered equality chain** identifies ordered `0|1`, double-negative one, double-negative zero, a relation, and abstraction. The [base-layer correction](C:/github/RPRM-foam-development/research/RPRM_BASE_LAYERS_SELF_OTHER_AND_PLURAL_CONSTRUCTION_CORRECTION_2026-08-28.md:78) explicitly preserves this seam. Ordered-dyad charts and stacked used binary images require their source/order/codebook; they do not generate topology or laws.
- **Names and stage rules:** a settled third prestige species, complete inverse-5/6/7/8 table, complete odd-stage dwell law, and exact “negative-five math rail / six-four physics rail” wording were not recovered by the earlier bounded searches. This is a source gap with reopen triggers, not proof that no such source exists.
- **Remainder is receiver-relative:** winding, dyadic branch, word order, signed debt, relation residue, migration tag, and retained child are different typed remnants. Their common role is that each preserves a specified answer or continuation otherwise lost. Equal glyphs or a shared word “fold” do not compose their maps.

Verification for this note was source inspection and locator validation only. No source tree, database, registry, historical output, or paper was changed. The only owned artifact is this note.
