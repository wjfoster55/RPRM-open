# Figure review

12 September 2026. Root generated and visually inspected the final
3200 by 1500 PNG after revising the limiting-endpoint annotation.

- The left panel retains the single shared link between two loops. It is
  a schematic of actual link occurrences, not a replacement of the full
  SU(2) configuration geometry by a flat pair of squares.
- The middle curves are exact certificate formulas. If x is r divided
  by 1/(48m), then t=x/6,
  Y=(3/8)(1-sqrt(1-8x/9)), and the lower gap curve is 1-2Y.
  At x=1 they give Y=1/4 and gap>=1/2. At the strict contraction boundary
  x=9/8 the limiting values are 3/8 and 1/4. Open circles identify this
  limiting endpoint; it is not silently admitted by Banach contraction.
- The right panel's size axis is explicitly schematic. The shaded strip
  depicts the safe headline bound for all finite sizes. The grey region
  is labelled outside that certificate, not gapless. The small extension
  beyond the headline strip is visible in the middle panel's strict bound.
- Labels, formula thresholds, legend, source caption and annotations are
  visible without clipping. The revised annotation avoids the lower curve.
  The complete figure fits the exported canvas.
- The footer states the fixed-lattice scope and the remaining continuum
  obligations. No computed spectrum, physical surface, simulated field
  or spatial embedding is claimed by the plotted curves.

The SVG and drawing source are included. Regeneration uses already
available Matplotlib; portable mathematical replay needs only Python's
standard library. Image byte and dimension checks are separate from this
visual review.
