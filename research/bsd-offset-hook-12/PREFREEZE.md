# Offset-hook12 — candidate freeze

User proposal: a circular pi reference locks2.0340, interpreted as
'Prestige Pi'; following digits are offsets or overlap hooks;010 might
compress0012300 or01230, and123 compresses to4.

The already specified seven-state pi decoder is reused at its exact
scope. No all-purpose meaning of 'closure' or bare digit is assumed.
The user's full grammar remains unspecified. Test these explicit candidates:

H1. On direct codes c=(delta,2delta,A), midpoint A+delta maps123 to4.
Enumerate all seven fibers and their inverses where unique.

H2. Half-gap projection maps123 to1. With one zero at each frame boundary,
01230 maps010. Normalize additional external zero runs only while retaining
their counts. Test complete delta-only fibers, then add independent anchor A
to recover c. The pi record can supply A=3 but is not the only such source.
With the entire key(A,m), delta and c are already fixed.

H3. An alternative composition uses old residual view(0,A−2delta), then
maximal suffix-prefix overlap:0 overlap01 overlap0 =010 at target123.
Keep signed residuals, distinguish this newly selected overlap law from H2,
and retain frame counts for a promised original-word inverse.

H4. Align the exact distance against2.0340: d=a+10^-4*r4. Also use the
equal-valued three-place anchor2.034 with residualr3. Prove r4=10*r3.
Read the seven-character suffixes after2.034 and the six-character suffixes
after2.0340; the former include the zero shared with the four-place anchor.

H5. Refine the actual height from depth9 to depth10 using the unchanged
rational kernel and its proved tail. Test the narrow persistence candidate:
does the next lower-bound suffix again contain the same framed123 hook?
Failure rejects this fixed-boundary persistence candidate, not every future
scale-dependent user grammar. Bounds are two estimates of one value.

H6. Exact finite circular phase and overlap checks on piword31415926535,
0012300,01230,0104820,010. A wrap of the finite pi window is declared,
not a claim pi's infinite expansion repeats. Keep occurrence orientation,
marked origin and any whole-turn counter. Closure alone does not prove
an unrelated real BSD residual vanishes.

All executable source, fresh evidence and unresolved obligations go in this
new experiment directory. No old research lane, registry or source archive
is changed. Evidence grades remain theorem dependency, written deduction,
finite experiment, proposed interpretation or OPEN.
