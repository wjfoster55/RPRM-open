# Second steering: two complementary openings

The user clarified that “69” names a shape, not the decimal digits six
and nine: the suffixes may describe two half-open closures that fit
together. This supersedes treating literal persistence of 123 or 010 as
the main hypothesis. Those earlier tests remain evidence about narrower
candidates only.

The previously calculated anchor is 2.0340. The user's later 2.0430 is
treated provisionally as a transposition of that anchor; no new numerical
distance or decoder is inferred from it.

Before the new finite tests, freeze two candidate realizations:

1. Complementary gaps. For one certified interval [L,U] and a point x in
   it, the two oriented gaps x-L and U-x join to U-L. Retain both signs,
   scale, endpoint roles and shared x. Determine what this joining law
   preserves under refinement and whether it selects x uniquely.
2. Two transport legs. One opening carries a state by f and the other
   carries it back by g. A complete operational circuit requires g(f(x))=x
   with both operations enabled. Solve this exactly for rational affine
   maps and retain inconsistent, unique and nonunique cases. A drawing
   of two joined paths supplies endpoint incidence, not the transport law.

These are proposed mathematical models of the user's shape intuition,
not declarations that the user's intended rule has been identified.
The finite test uses seven interior points of each actual height enclosure
and 625 affine coefficient tuples on a declared nine-point test carrier.
The separate written affine proof covers rational states with its stated
domains. No new height computation or imported numerical BSD value is
needed. The full RPRM-to-BSD comparison remains an open obligation.
