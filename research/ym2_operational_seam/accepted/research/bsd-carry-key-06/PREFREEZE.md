# Candidate contracts before the new computation

Question: can William's ordered 426 / 625, fiving and pi-key reading
identify an operation useful for the BSD comparison?

The supplied occurrence is a coefficient residue together with its modulus:
`b2 in 426+625 Z5`, under the exact conventions of experiment 05. The two
integers are not a numerator and denominator of an ordinary fraction.
Prior overconvergent evidence already contains higher precision; this is
an independent refinement and operational audit, not a blinded prediction.

Frozen candidates:

1. Decimal reversal and increment: `rev10(r)+1=5^N`. It holds for
   `(r,N)=(426,4)`. Test all five canonical lifts modulo 3125, and freshly
   calculate the next coefficient digit. Base, width and the canonical
   representative are explicit ports.
2. A proposed centered chart with `g=2`, `1<=m<=7`, lower endpoint `m-1`,
   upper endpoint `B=m+1`, and outer value `c=m+2`. Encode one state as
   `(m,2,c)` and another as `(c,2,B)`. Enumerate all seven states, prove
   the inverse, and test which second words are positive powers of five.
   This role assignment is a new candidate, not a recovered meaning of
   either BSD occurrence. Separately, the historical endpoint-gap chart
   interprets 426 as `(A,g,B)=(4,2,6)` and reflects it to 325.
3. Multiplication by the coefficient on `Z/625`: test whether it retains
   the first two base-five digits, determine complete cycles, and retain
   what remains after five multiplications when the coarser state closes.
4. Compare integer addition by five with a units-digit half-turn holding
   the decimal decade fixed. Test descent from integer representatives
   to `Z/625`, and test the explicit repair retaining one parity bit.
5. Audit the chosen logarithmic coordinates `6=1+5` and `-4=1-5`.
   Their product is `1-25`; the log sum therefore retains one additional
   factor of five. Transport the quadratic coefficient by the square of
   the coordinate conversion, without asserting it supplies a BSD scalar.

These candidates are not exclusive interpretations. Their source ports
and requested observations differ. The mathematical receiver asks for
exact maps, inverses, full finite fibers and continuation behavior. A
general BSD identity remains a separate proof obligation.

Fresh analytic work uses the same exact normalization and classical
measure error proof as experiment 05, with residue-ball level extended
to 6. Internal precision is 20, analytic error is O(5^7), and division
by the known logarithm squared leaves five certified coefficient digits.
The same exact symbol also supplies an overconvergent cross-check.
