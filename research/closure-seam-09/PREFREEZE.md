# Seam 09: relational datum and distance candidate

User steering: the seam may be a relation that neither side has individually, perhaps distance or the combined carry. Treat this as a candidate for testing, not a supplied identity. This successor follows bridge 08 without changing old lanes.

Frozen work:

1. Distinguish a datum missing from either side individually from one missing even from their retained pair. Use a complete two-matching finite example; preserve joint occurrence identity.
2. On E34, compute P-Q independently by the rational group law from P=(-2,48), Q=(-16,120). Declare canonical-height squared distance D=H(P-Q), not Euclidean distance between the plotted rational coordinates.
3. Freshly compute certified intervals for full canonical heights of P,Q,P+Q,P-Q with eight exact projective doublings, the prior written all-point tail constants C0=5345344 and C1=1338649, and a new rational logarithm enclosure. Check early doublings independently by the chord law; include O and the three two-torsion points.
4. Recover the arithmetic pairing and regulator through the distance formula. The theorem supplies the parallelogram law; overlapping numerical intervals are only a consistency check. No analytic coefficient, Sha order or BSD quotient is an input or an expected target of this calculation.
5. Test reversal Q->-Q and unimodular shears Q->Q+nP. A universal absolute distance threshold must survive these representation changes if it purports to describe the unchanged elliptic lattice/BSD target. A named basis-dependent receiver is a different contract.

Logarithm enclosure: normalize positive integer H=2^e r, bound r by 96-bit dyadic endpoints in [1,2], and bound log r using 48 positive atanh-series terms plus its geometric remainder. Carry the error outward through all rational operations. Decimal interval outputs are rounded outward. These numerical bounds do not establish exact identities by matching decimals.

Writes are confined to research/closure-seam-09 and sibling delivery files. No old campaign, paper, other-task message or background automation is started. Separate scoped proofs, primary-source dependencies, finite tests and the remaining analytic-arithmetic bridge.
