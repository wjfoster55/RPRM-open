# Seam 09 evidence provenance

The user's current request proposes distance or a joint relation as a seam that neither side possesses separately, and authorizes further tests. This new pass specializes that proposal. It uses the already-read RPRM math-lenses workflow and the current repository's precise carrier/receiver rules. No new broad corpus or Memory Fabric search was needed: bridge 08 and the selected current source passages supplied the relevant context.

Only this new directory and its sibling delivery archive/receipt are written. No existing research lane, paper, registry, separate task, automation or goal was changed. Historical instructions are source evidence, not new authorization. Internal bounded subagents produced the joint-relation note, geometry audit, numerical review and schematic.

## New calculations

`work/check_distance.py` computes E34 group addition, eight projective doublings of P,Q,P+Q,P-Q,O and the three two-torsion points, rational logarithm enclosures, canonical-height tail intervals, polarization and determinant intervals. No old executable or old numerical value is imported. The first three doublings of every source are independently compared with the chord law; all eight use the declared exact projective formula and retain gcd reduction. Point admission and `(P-Q)+Q=P` are checked exactly.

The bound on the infinite remaining height tail is an explicitly attributed written theorem from `research/bsd-identity-01/EXPLICIT_IDENTITY_TARGET.md`, equations (6)–(11), with fixed constants and exceptional cases. Its correctness is not inferred from the new finite gcd checks. Quadraticity, nonnegativity and torsion behavior are published height-theory dependencies. The new rational log enclosure and interval-rounding arguments are written in `DISTANCE_AND_SEAM.md` and independently reviewed in `agents/NUMERICAL_REVIEW.md`.

The source computes all 625 two-by-two integer matrices with entries -2,...,2 against a fixed positive rational Gram model, including 104 unimodular matrices. The general determinant law is a separate written algebraic argument. The E34 shear intervals are transported through the proved quadratic-height law and are not new independent height evaluations; repeated interval occurrences can widen them conservatively. The n=1 difference height is exactly H(Q).

`work/check_joint.py` separately enumerates the entire 32-source finite matching carrier. It computes distances directly and by the expanded cross-term formula, retains all source rows, and checks the minimum binary repair for its chosen fiber. This is not an elliptic-curve computation. A distance-only receiver can forget occurrence distinctions that another receiver needs.

The result JSON files bind their current checker sources by SHA-256 and record execution times. Source hashes bind bytes only. The main interval execution completed successfully; its source/rounding review found no defect requiring a repeat. The numerical example's field `area` is parallelogram area, not triangle area.

## Definitions and primary sources

The old E34 target fixes the minimal model, integral basis, full x-height, period over both real components and remaining exact identity. These are inherited, attributed dependencies; this pass does not independently re-prove analytic rank, basis saturation, the published full-height theory or any full BSD case.

The geometry audit checked primary sources:

- [Cremona, Algorithms for Modular Elliptic Curves, chapter 3 §3.4](https://johncremona.github.io/book/fulltext/chapter3.pdf), for height and regulator convention.
- [Milne, Elliptic Curves, IV §§4 and 6](https://www.jmilne.org/math/Books/ectext6.pdf), for the quadratic height, torsion kernel and pairing. The audit explicitly adapts his unhalved polarization to the half-polarization used here; notation alone was not assumed to match.

The Gram-fiber descriptions in `DISTANCE_AND_SEAM.md` and `agents/GEOMETRY.md` cover all real positive-semidefinite two-by-two Gram matrices. They are not a complete enumeration of realizable rational-point configurations on E34. The exact arithmetic distance is a metric after quotienting torsion, and a pseudometric before that quotient.

RPRM source connections and exact line locators are retained in `agents/JOINT_RELATION.md`: `docs/core.md`, `docs/operations.md`, `docs/proof-donut.md`, the current handbook, the bridge-08 corpus map, and selected recovered-concept passages. The prior campaigns cited by those recoveries were not rerun or promoted.

## Figure and delivery

`work/draw_joint.py` renders the fresh finite receipt with matplotlib, checking its source binding before drawing. Reading that receipt supplies the picture, not the truth decision in either mathematical checker. Both the subagent and root visually inspected the PNG. The figure states that its arrows are pairings, not geometric distances. The SVG is included for export.

`evidence/FILE_BINDINGS.json` records source and dependency hashes at closeout. `MANIFEST.json` covers the delivered files; the sibling archive receipt verifies every member and exact source-byte equality. No saved PASS record, matching decimal approximation, database rank or conjectural Sha value is used as proof.

**Remaining obligation:** construct and prove the analytic-to-arithmetic comparison with its stated normalization, coverage and boundaries, and establish the actual required Sha identification. This pass closes the declared finite matching fiber and provides fresh rigorous arithmetic enclosures. General BSD and the remaining Millennium targets are not claimed solved.
