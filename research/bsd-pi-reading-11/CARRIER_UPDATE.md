# Derived adapter after the fresh depth-nine result

The first test was frozen in PREFREEZE.md and retained in evidence/reading.json.
It proved d lies in[2.034001230068,2.034010481998]. This new information
enables a stronger consequence: literal leading fractional digits0340,
and thus hundredths/thousandths digits34, are certified.

Define, now explicitly, the bounded real carrier X=[2,2.1), the finite
digit receiver f(x)=(floor(100x) mod10,floor(1000x) mod10), and the old
seven-state endpoint-midpoint chart followed by its D0 word decoder.
This is a derived coordinate choice suggested by the user's34 reading,
not a user-confirmed unique interpretation or a prediction frozen before
seeing the old number. X is infinite; the receiver has100 possible pairs.

The composition is defined only on the seven admitted pair states. On
the rest, return an admission error. Its complete pi-window fiber in X
is[2.034,2.035). Many x give the same finite word. This is the fiber of
the decoder, not the uncertainty interval for the actual E34 distance.

This adapter reads two positions of one real value. The alternative
lower-bound/upper-bound construction reads one aligned position from
two bound words. They are different operations; the new lower bound
excludes the old straddling of2.034.
