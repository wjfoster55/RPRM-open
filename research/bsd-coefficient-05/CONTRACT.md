# Scope, types and evidence

The user authorized actually computing the independently normalized
coefficient, testing dimensional alignment, reopening the prior prime-family
atlas read-only, and displaying the two scales. This directory is a new
bounded BSD experiment. Other research lanes and earlier experiments are
unchanged; historical source instructions remain attributed evidence.

The exact modular-symbol carrier is the plus cuspidal subspace of weight2,
level 32 over Q. Its dimension is 1. The path from infinity to0 is fixed to
1/4 by an unconditional exact L-value identity, not by a desired BSD result.
The quadratic character is the primitive Kronecker character of fundamental
discriminant 136, with value 1 at 5. Its translation sum has a specified factor 2
relative to the connected real period of the minimal E34.

The analytic carrier is a bounded ordinary measure on Z5 units and its
power series on the trivial Teichmüller branch. Equality of finite readouts
is congruence with a declared absolute 5-adic precision. The reduction map
from Z5 to Z/625 has the complete fiber MANY(426+625Z5) over the returned
residue. The uniquely defined analytic coefficient belongs to this fiber,
and every member is nonzero. No finite readout claims to recover the exact
unrestricted scalar.

The classical measure admits every residue unit a modulo 5^n. All balls at
each selected level are enumerated, with their complete character sums.
The returned integral has a written uniform error bound. A zero residue
means divisibility by the reported power; exact vanishing of the first two
coefficients is a separate cited theorem.

The real comparison uses c=L''/2 and Omega_all·Reg_full. The 5-adic
comparison uses degree-two augmentation tensors, or their explicitly chosen
scalar coordinates. They are not divided across real and 5-adic fields.
Period ratios, twist scale, factorial, logarithmic coordinate and height
normalization are retained in every conversion.

The displayed prime-family counts are recovered finite results attributed
to Audits 102/103. Newly executed prime counts at 5,13,41 are explicitly
separated. Neither an atlas label nor agreement after common Euler-factor
multiplication determines the analytic coefficient.

Evidence grades: exact rational modular-symbol construction; written
theorem-based normalization; fresh finite computation; an independent
analytic error argument; and explicitly open global comparison obligations.
This is not formal verification of PARI or Python. Hashes bind source bytes.
