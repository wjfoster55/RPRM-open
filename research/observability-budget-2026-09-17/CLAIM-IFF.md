# OB-iff: when extra time refines Q

17 September 2026. Last typed cut of this branch. Nulls frozen in
[NULL-IFF.md](NULL-IFF.md) before Q14–Q16 ran.

## Task record

1. **Carrier.** L as in CLAIM.md. M is `{0,1,2}` with identity action.
   Hostile N is the 8-state shift, panel `{Y}`, `Q=c`.
2. **Ports.** Functions `X→O`. Representation `C_S` and its traces.
3. **Operation.** Total `tick`, `id`, `shift`. Q14 excludes `reveal`.
4. **Receiver.** L: `Q=h`. M: `Q=[s=1]`. N: `Q=c`.
5. **Fiber.** Weak refine and repair as in NULL-IFF.md. Report NONE/ONE.
6. **Hostile / grade.** `{Y}`: descent can fail at horizon 1 without
   refining Q. Written iff plus finite check. Not Lean. Not AD-R3.

## Claim

Extra time refines Q exactly when a later reading splits a present
Q-ghost. If `C_S` is a dynamical homomorphism, that never happens. If
it is not, it still may not happen at a given horizon. After the check,
stop padding.
