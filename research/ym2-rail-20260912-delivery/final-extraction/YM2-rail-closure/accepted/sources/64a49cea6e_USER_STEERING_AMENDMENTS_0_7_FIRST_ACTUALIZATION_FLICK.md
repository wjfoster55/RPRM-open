# User steering amendments 0.7 - first actualization flick

**Date:** 2026-08-12  
**Status:** source steering plus recovered finite donor; not a new passed result  
**Relationship:** additive to amendments 0.4 through 0.6

## New steering

William corrects a serial reading of the keyhole mechanism:

- it is a **flick**;
- it is a flick **with a plus one**;
- inserting the coin may itself perform everything provisionally called MOVE,
  VERIFY, MAKE, and OUT;
- gauge, reciprocal handshake, forming/stamping, release, and exterior
  actualization may be typed faces of one atomic physical event rather than
  mutually exclusive machines;
- the target is the first actualization of the frozen construction.

Do not turn those faces into an interior instruction sequence. The event is one
strong relation occurrence with several exterior receivers.

## Recovered source lineage

This is not a new free metaphor. The local longitudinal audit records the
following exact William-origin lineage:

- repeated `scoop -> flick -> scoop -> flick`;
- hammer/anvil with useful output plus trash;
- one carrier scooping at one interface and unloading at another;
- past and future trash may both be required;
- one contact can update two coupled components;
- past-output feeds future and future-output feeds past at one actualization
  contact.

Source:
`RPRM_LONGITUDINAL_INTUITION_AND_MISSED_SEAMS_AUDIT_0_1_SHADOW.md`,
SHA-256
`6D7087D382DE0F73F50C6AB4134C0D4B95FA515DC3B13D7367AE0A7C1B7F195B`,
especially sections 3.2, 3.3, 6, 7, and 10.

That audit separates useful output, signed numeric residual, provenance/history
residual, and physical exhaust. They may all leave one contact but must not be
identified by the word "trash."

## Exact finite donor: Double Stamp

The passed `RPRM_DOUBLE_STAMP_SEAM_MIDPOINT_01` supplies a close formal donor:

```text
A5: one center vertex, STATE
B6: inverted center edge, EVENT, dwell forbidden
C7: inserted fixed midpoint, STATE
```

The midpoint insertion adds one raw vertex and one raw edge while the quotient
gains one vertex and no edge orbit. It removes the edge-inversion receipt. The
event has exact rational clock coordinate `1/2` between source `0` and target
`1`, but the bench makes no physical-duration claim.

This gives a finite precedent for:

```text
open/inverted seam + admitted midpoint occurrence
    -> one non-dwell flick event
    -> repaired/actualized state with a typed plus one
```

It does not prove that a physical coin, keyhole, or Patient Zero uses this
carrier.

## Candidate first-actualization relation

Freeze rival expansions of one event, not a pipeline:

```text
PRE:
  open typed relation R with APERTURE a
  admitted exterior occurrence coin c
  retained pre-boundary receipt

FLICK:
  one live reciprocal contact seam F(a,c)
  no dwell and no semantic branch

POST:
  closed incidence retained strongly
  receiver-visible occurrence tau or closed state
  next/open OUT role a'
  any useful output, routed residual, lineage residual, and physical exhaust
  separately typed
```

The exterior `+1` receiver may count one admitted occurrence, one midpoint, one
new quotient state, one emitted ticket, or one grade lift. These meanings must
not switch silently.

## What "first" may claim

`FIRST_ACTUALIZATION` means first relative to a fully frozen finite carrier and
its pre-state. It is not a cosmological first event, creation from nothing, or
universal origin law.

The pre-state, admitted coin, complementary aperture, and supplied energy are
not nothing. The experiment asks whether one actualization event can be made
native, atomic, directly refeedable, and receiver-complete without an interior
controller.

## Discriminators

Compare:

1. coin merely counted but seam remains live;
2. coin completes fit but no exterior ticket actualizes;
3. boundary die forms the output during passage;
4. one atomic fit both closes the seam and emits the same ticket type;
5. semantic controller performs the same visible transition;
6. output is correct but residual/lineage needed for another flick was lost;
7. output re-enters unchanged and produces a second homologous flick.

The strongest current candidate is row 4 followed by row 7. Rows 1 through 3
are partial constructions; row 5 is the top-down control.

Feed this amendment into the active independent lanes and later blinded
reconciliation. Do not edit sealed capsules, fabricate apparatus, or alter
NariBrain from this source note alone.
