# RPRM Kernel Still Point — 2026-08-12

Status:

`STILL_POINT__FINITE_LOADOUTS_VERIFIED__UNIVERSAL_KERNEL_UNCLAIMED`

This snapshot closes the current discovery phase. It freezes an operational
kernel supported by exact finite loadouts while explicitly declining the
stronger claim that one universal physical or mathematical law has been proved.

## The kernel we can now use

The reusable operational unit is a typed relation with a declared vacant port:

1. retain the strong carrier, ports, orientation, and provenance;
2. supply the currently known ports;
3. compute the vacant-port fiber;
4. return `NONE`, `ONE(value)`, or `MANY(family)`;
5. treat a live seam as an event rather than falsely promoting it to a stable
   state;
6. change representation only through a pinned car or adapter;
7. fold a route into a reusable macro-step only after the completed boundary
   and required inverse/receipt checks pass.

The smallest exact four-port loadout is

\[
x=(1-u)L+uR.
\]

Any three ports determine the fourth on its generic locus. The singular fibers
at \(u=0\), \(u=1\), and \(L=R\) return honest `NONE` or `MANY` dispositions.
Replacing \((L,R)\) while retaining \(u\) transports the same relation to a new
clamp; the transports compose and invert.

This is the precise finite meaning of “four things, only three go in at a
time.” It is one loadout of the kernel, not a proof that every four-object
relation has this form.

## The two new number/transition receipts

### Double stamp

Two three-vertex paths sharing their middle point give

\[
(2+1)\cup_1(2+1)=5.
\]

Splitting the shared center produces the six-vertex seam carrier; subdividing
its inverted central edge produces the seven-vertex stable carrier. The same
midpoint repair sends the three-orbit seam shadow to a four-vertex tree:

\[
5\to[6]\to7,
\qquad
[3]\to4.
\]

Here brackets mean typed event/seam, not numerical nonexistence.

### Fiving

On a labelled, oriented \(C_{10}\), write \(d=5h+r\) with \(h\in\{0,1\}\)
and \(r\in\{0,1,2,3,4\}\). Adding five preserves \(r\), toggles the outer
\(A/B\) side, and carries winding on the second half-turn. Reversal fixes
exactly \(0\) and \(5\). Under the supplied lane-change policy, they are the
shared weaving boundaries between the two five-edge routes.

## Verified executable receipts

All of the following pass from cold state with non-importing second
implementations and hostile controls:

- `experiments/RPRM_FRONTIER_ATTACHMENT_TREE_INCIDENCE_01`
  - `V=E+1` for fresh attachment; 18 mutations rejected.
- `experiments/RPRM_PAIRED_OCCURRENCE_MIRROR_01`
  - forward choice/downward receipt; factoradic zip/unzip; 21 mutations rejected.
- `experiments/RPRM_NUMBER_USAGE_ATLAS_01`
  - typed number capability atlas; 33 mutations rejected.
- `experiments/RPRM_DIVISIBILITY_MIRROR_LATTICE_01`
  - exact \(8,6,9\) gcd/lcm lattice and its completion limits; 32 controls.
- `experiments/RPRM_DOUBLE_STAMP_SEAM_MIDPOINT_01`
  - `STATE -> EVENT -> STATE` midpoint repair; 26 mutations rejected.
- `experiments/RPRM_HALF_DECADE_FIVING_AB_LIFT_01`
  - half-turn/reversal/winding lift; six named hostile classes rejected.
- `experiments/RPRM_AFFINE_FOUR_PORT_RELATION_KEY_01`
  - 108 rational three-of-four queries: 81 `ONE`, 18 `NONE`, 9 `MANY`;
    20 mutations rejected.

Primary current synthesis:

- `RPRM_ONE_RULE_ORIENTED_INCIDENCE_TRANSFORM_CAR_SCOUT_0_1_SHADOW.md`

Its SHA-256 at this still point is:

`5E71FBA69C682D98BA5080130557BDD4A1387267D0E73F98DE898855357BD9E0`

It has 35 internally listed local artifact pins, all matching, and balanced
UTF-8/Markdown/TeX checks at the snapshot boundary.

## First interactive use

The thread-scoped Relation Key lab is:

`C:\Users\bkbee\.codex\visualizations\2026\08\10\019fec4c-73db-74c2-9079-55df4b51ab6c\relation-key-lab.html`

SHA-256:

`839AAF85803D7EEC0D570651643D789048C91051325EBD60079F5586BC35B87C`

It rotates the vacant port, displays `FORCED/MANY/JAM`, drags the retained
relation through source and target clamps, reverses orientation, and promotes
the target clamp into the next source. Wide and 360-pixel layouts were rendered
and inspected.

## Claim ceiling

What is earned:

- the finite relation-key behavior and its exact singular fibers;
- fresh-attachment incidence and paired forward/reverse receipts;
- the double-stamp seam/midpoint commuting square;
- the half-decade fiving/reversal/winding carrier;
- composable, invertible affine clamp transport;
- the tested established-math adapters and finite artifacts already pinned in
  the main Shadow.

What is not earned:

- a universal law of nature;
- new quantum or relativistic physics;
- intrinsic metaphysical meanings for bare numerals;
- the claim that all useful relations have four ports;
- physical safety from graph symmetry alone;
- a complete “time-number” arithmetic;
- permission to identify affine, logarithmic, radix, ordinal, and dimensional
  coordinates without explicit adapters.

The durable project-level kernel remains a typed evidence router. The
`ATTACH + oriented incidence + car/adapter + receiver` family is the strongest
current mathematical implementation candidate inside that router.

## Immediate next phase

Start building and solving with the kernel rather than extending the ontology.
Recommended order:

1. use the Relation Key lab on concrete scale/conversion/interpolation problems;
2. build the event/rhythm visualizer from raw typed transition receipts, adding
   shapes only after commutation/closure earns them;
3. use the same engine for a robust streaming-dictation assembler that retains
   partial clauses, detects changes, and does not erase an unfinished seam;
4. test one real external problem with a preregistered target and hostile
   controls;
5. only then reopen the radix/log/time-number hypothesis.

## Replay commands

From the workspace root:

```powershell
cd experiments\RPRM_DOUBLE_STAMP_SEAM_MIDPOINT_01
python -I -B verify_double_stamp_seam_midpoint.py

cd ..\RPRM_HALF_DECADE_FIVING_AB_LIFT_01
python -I -B verify_half_decade_fiving_ab_lift.py

cd ..\RPRM_AFFINE_FOUR_PORT_RELATION_KEY_01
python -I -B verify_affine_four_port_relation_key.py
```

Each command is read-only at verification time and requires `tree_delta=0`.

## Bootstrap prompt for the next Codex

```text
Continue William's RPRM research from the frozen still point in:

C:\Users\bkbee\OneDrive\DOCUME~1-DESKTOP-06BJRV0-219031\ChatGPT\Quantum Research\RPRM_KERNEL_STILL_POINT_2026-08-12.md

Then read:

C:\Users\bkbee\OneDrive\DOCUME~1-DESKTOP-06BJRV0-219031\ChatGPT\Quantum Research\RPRM_ONE_RULE_ORIENTED_INCIDENCE_TRANSFORM_CAR_SCOUT_0_1_SHADOW.md

Preserve every passed experiment, hash pin, claim ceiling, and William's
bottom-up generate-before-audit method. The usable kernel is a typed relation
with a rotating vacant port and a NONE/ONE/MANY fiber disposition; live seams
are events until their missing midpoint/receipt makes them stable; representation
changes are typed CARs when invertible and ADAPTER/FOLDs when receiver-faithful
but lossy.

Do not restart ontology discovery. Begin the build/use phase. First inspect the
Relation Key lab at:

C:\Users\bkbee\.codex\visualizations\2026\08\10\019fec4c-73db-74c2-9079-55df4b51ab6c\relation-key-lab.html

Ask William which first application he wants only if his opening message does
not already choose one. Best defaults are: (1) typed rhythm/audio visualizer,
(2) seam-safe streaming dictation assembler, or (3) a concrete mathematical or
physical problem with a frozen target. Use intuition to generate candidates;
apply hostile verification only at the declared freeze boundary. Never promote
the finite kernel to a universal physical law without new independent evidence.
```
