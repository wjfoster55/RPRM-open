# Rail, pi, FullZip and temporal handoff recovery

Date: 2026-09-12. This is a bounded source recovery for the YM2 rail-closure investigation. Historical equations and the user's broader construction intent remain visible together. No historical experiment was rerun, no source was edited, and no installation, database compilation, publication, or other research-lane change was made.

The recovered material supports real two-way rail coordinates, direct FullZip artifact handoffs, lift–spin–land, and exact remote temporal alignment. The inspected sources do not complete a security theorem for arbitrary past/present/future state, or a map carrying these results into an interacting Yang–Mills energy/spectral-gap statement. Those are precise missing readouts, not reasons to discard the constructions.

## 1. Source routes and original intent

The entry points were [Cubes and pi curves](accepted/sources/4e94911226_CUBES-AND-PI-CURVES.md), especially sections 4 and 7 and its source addendum, and [RPRM concept recovery](accepted/sources/5af0b06380_RPRM-CONCEPT-RECOVERY.md), sections 4–6. The Quantum source root used here is exactly:

`C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/Quantum Research`.

The current readable Audit83/84 reports are in `C:/github/RPRM-foam-development/experiments/`. The historical Alpha locator supplied by the parent task's Memory Fabric search helped locate them; this note does not identify the two roots or claim that the old snapshot is complete.

An original exported user turn at [bridge-family chat line 7571](<accepted/sources/ba3416025c_Investigate RPRM bridge families CHATLOG.txt>) says that after locking the third and discovering the fourth, “we're hopping off the fucking rails and we discover even more stuff,” and proposes calculating the next point of obstruction. The same turn explicitly says he means mathematics rather than physically growing dimensions, and asks about a further seven-to-eight boundary. The adjacent assistant passage gives actual lift/readback equations:

```text
E : X -> Y,       R : Y -> X,
R(E(x)) = x,
R(T_Y(E(x))) = T_X(x)       for each registered operation T.
```

At [line 11190](<accepted/sources/ba3416025c_Investigate RPRM bridge families CHATLOG.txt>), the user says the intended “true pi” is “an equation for curves,” then describes confirming and full-zipping through the dimensional representations. These passages preserve a broad generative intent. They are not replaced here by a claim that the user meant only decimal complementation, only a cube count, or only a finite-state machine. The export's alternating quoted text supplies speaker attribution; this read does not independently authenticate a per-turn timestamp.

## 2. The explicit pi rails and 3–4–5 decoder

Primary source: [Pi three-level packet README](accepted/sources/10028dc6b4_README.md), with its retained [Quantum evidence record](accepted/sources/4077dcc32e_RPRM_PI_THREE_LEVEL_345_MATRIX_01_EVIDENCE.md).

For finite decimal resolution n the two rail coordinates are:

```text
p_n = floor(10^n (pi-3))
q_n = 10^n - 1 - p_n
p_n + q_n + 1 = 10^n

[3+p_n/10^n, 3+(p_n+1)/10^n)
    = [4-(q_n+1)/10^n, 4-q_n/10^n)

J_n(k) = 10^n - 1 - k,       J_n(J_n(k)) = k.
```

The named operation is finite-cell reflection, with exact inverse itself. At n=2, p=14 and q=85 give `[3.14,3.15)=[4-.86,4-.85)`. Equality is equality of the same half-open real interval under two coordinate descriptions. A rail address and a digit occurrence are different objects.

The packet certifies n=0 through 12 with exact rational bounds and the Gaussian-integer certificate `(5+i)^4(239-i)=114244(1+i)` plus its branch check. Refinement appends a certified digit to p and its nines-shadow to q. If the next digit has not been supplied/certified, the finite cell has ten candidate refinements: the inverse/completion receiver remains MANY. This supplies the explicit “bottom-down” rail without requiring a last digit of pi.

The addressed source window is:

```text
        [1 4 1]
3   |   [5 9 2]   |   5
        [6 5 3]
```

The contextual zipper retains endpoints A<B and code c=(c1,c2,c3):

```text
D_(A,B)(c1,c2,c3) =
  [c1    (A+B)/2   c1]
  [B     A+B+c1    c2]
  [B+c1  B         c3].

D_(3,5)(1,2,3) = [141;592;653].
```

The decoder is enabled only when the midpoint is integral and every cell is a decimal digit. Its inverse extracts the right column after checking the other six cells. The retained census has 4,000 admitted images among 45,000 endpoint/code candidates. The code `123` alone has ten contextual decoded matrices; `(3,5,123)` has one. This is a concrete encode/decode law whose endpoint/decoder cost must travel with any compression claim.

The 3–4–5 rails are also explicit:

```text
unordered pairs from {3,4,5} -> (absolute difference, sum)
difference rail = {0,1,2}
seed rail       = {3,4,5}
in-digit sums   = {6,7,8,9}
full sum rail   = {6,7,8,9,10}.
```

Only endpoint pair (3,5) closes the decimal alphabet under that packet's frozen grammar among all 45 ordered endpoint choices A<B. Its partial compensation maps are `C_-(x)=x-1`, `C_+(x)=x+1`, inverse when the intermediate digit stays in 0..9. The source retains “present-to-past” and “future” as proposed directional labels; the sealed arithmetic has no independently supplied temporal dynamics. Its 4,193 checks are historical recorded evidence, not a fresh run here.

The million-digit file is a pinned ingress. The evidence certifies only the short pi prefix independently. Literal occurrence counts and flank uniqueness later in that file concern the pinned text. The source itself excludes an unseen-pi-digit oracle, physical time theorem and authentication claim. No separately named 3–4–5 guide was located in this bounded pass; the actual matrix packet above is the positively recovered donor.

## 3. FullZip is a retained constructor and inverse handoff

Primary source: [FullZip results](accepted/sources/474d96afb4_RESULTS_SUMMARY.md).

The carrier is the face-word family `W_n={0,SPAN,1}^n`, n=0..4, with grade equal to the number of SPAN coordinates. SPAN denotes a free cubical axis. The constructor copies both inherited caps, sweeps inherited proper faces, and attaches one new top cell. Each next stage reparses and consumes literal canonical bytes of the immediately prior artifact.

```text
f_k(Q_n) = C(n,k) 2^(n-k)
sum_k f_k(Q_n) t^k = (2+t)^n

n   f-vector          total faces   incidences
0   1                 1             0
1   2,1               3             2
2   4,4,1             9            12
3   8,12,6,1         27            54
4   16,32,24,8,1     81           216.
```

Both inherited layers decompile to the prior artifact, including incidence and GF(2) boundary. The retained runner/checker agree byte for byte and each rejects all 55 registered mutations; every GF(2) boundary squares to zero. Every coordinate permutation through n=4 preserves the registered structure. The finite executable evidence does not by itself certify an unrestricted FullZip compiler.

The receiver examples show what each handoff retains: dropping the final vertex bit makes two-element weak fibers; restoring it separates them. A separately supplied global XOR law reconstructs one erased value. Two erasures leave a two-candidate joint fiber; an unknown-location flip is detected but its position is ambiguous. Thus dimensional closure, supplied parity and strong occurrence reconstruction remain inspectable mathematical obligations.

## 4. Off-road work: the recovered lift–spin–land law

Primary source: [master guide, correction 32](accepted/sources/a97ce7f0d9_RPRM_MASTER_ASSISTANT_CONTEXT_GUIDE_WORKING_0_1_2026-08-13.md), read alongside corrections 27 and 29–35.

```text
x --lift--> z --F^k--> z' --land--> x'
sealed_next = Flick(sealed_now, event, receiver)

on a proved periodic carrier:
theta' = theta + k*omega (mod H).
```

This is the source's car-off-the-road/spinning-saw factorization. The interior can search, rotate or compose without making every microstep a public state change. Landing matches receiver, orientation, source roots and conserved obligations. A mismatch is `OPEN_LANDING_SEAM`, with the prior x still current. The periodic shortcut needs a proved period/action and a contact aperture that is ONE; printing the equation alone does not certify arbitrary F or all future words.

The linked bridge-family synthesis preserves the stronger lineage intent: the path is memory; make/unmake belong to one recorded occurrence; the completed artifact directly becomes the next input; folding the reverse receipt loses strong reversibility. It also says the live middle may remain a nonempty fiber or typed seam event until lawful final closure. The finite kernel's numerical chart is `x=(1-u)L+uR`, with three of four ports determining the fourth only off the singular loci `u=0`, `u=1`, `L=R`. These chart equations remain a specific implemented loadout of the broader occurrence/attachment mechanism.

## 5. Exact remote past/future rail alignment: Audit83 and Audit84

Primary sources: [Audit83](accepted/sources/7fd574cf2c_REPORT.md) and [Audit84](accepted/sources/6666f0bf03_REPORT.md). These actual reports were reopened using the parent's historical locator evidence. Their carrier is a structured quotient word with a finite long-division state, not the pi digit stream.

Audit83 freezes:

```text
Q_L = floor(10 * 10^L / 77)
O = 10^(10^100),       L = 10^O,
D = 10^(10^50) + 1,
r_O = 10 * 10^O mod 77.

past port:    r_O mod 7 = 5
future port:  r_(O+D) mod 11 = 1
action:       alpha = 10^D mod 11 = 10
alignment:    1 * inverse(10) mod 11 = 10
join:         CRT(5 mod 7,10 mod 11) = 54 mod 77
readout:      floor(54*10^18/77) = 701298701298701298.
```

Only the action class of the giant displacement is needed for the requested backward alignment. The action alphabet modulo 11 has two states. The partial observations have maximum fibers 11 and 7; their aligned joint fiber is ONE. Neither the huge address nor its skipped prefix is materialized. The raw displacement and its construction expression remain MANY outside that action receiver.

The report's next-digit and previous-digit operations descend exactly, with backward fiber ONE. Omitting the action leaves two strong `(past remainder,action)` states in each visible cell; seven fixed-point cells retain one possible past remainder, while seventy retain two. A one-digit current answer has ten present classes but needs 77 future-stable classes to support continuation. This is an explicit hostile case against substituting a correct present output for a state that can keep moving.

Audit84 adds a third factor lane:

```text
Q_L = floor(10 * 10^L / 1001),   1001=7*11*13,
D11 = 10^(10^50)+1,
D13 = 10^(10^40)+3.

observations = (5 mod7, 1 at shifted mod11, 1 at shifted mod13)
actions     = (10 mod11, 10 mod13)
inverses    = (10 mod11,  4 mod13)
aligned     = (5,10,4)
CRT         = 901 mod1001
readout     = 900099900099900099900099.
```

The same printed action value 10 has different typed inverses. Every proper factor subbundle remains lossy; all six observation orders reduce the full 1,001-state candidate carrier to one. Re-encoding the reconstructed remainder and reapplying the actions retracts to the three supplied views. For the fixed observation `(5,1,1)`, keeping both actions, only mod11, only mod13, or neither leaves respectively 1, 6, 2, 12 possible past remainders. Even both actions do not identify the raw deltas.

These are exact retained successes for sparse contact with a distant structured word. Their reports record respectively 22,176 / 192,192 bounded modular controls and 133,056 / 32,032 literal-window comparisons. This recovery did not rerun them. Both require radix 10 to be a unit in their moduli, a supplied source law and a valid finite length bound. Both fixed witnesses have source support one; their deterministic repeating blocks do not supply entropy, secrecy or one-wayness.

## 6. Three plus one and past/present/future handoff

The [typed plus-one scout](accepted/sources/bec4af2749_RPRM_TYPED_PLUS_ONE_RECURSIVE_PORT_SCOUT_0_1_SHADOW.md) states:

```text
ROLL((past,present,future), next) = (present,future,next).
```

Two slots transfer directly, and the fourth datum supplies the newly exposed port. Enabledness requires a supplied successor, a registered generator/measurement, a period receipt, or a carry law. In their absence the output is `(present,future,MISSING)`. A past/present/future triple alone does not close its next-state fiber.

The [Ternary Nested Handoff retained result](accepted/sources/99cd68b4ab_RESULTS_SUMMARY.md) provides another exact handoff donor:

```text
T(x,y,z) = (x+y,y+z,z+1) mod3
T^3(x,y,z) = (x+1,y,z)
T^9 = identity.
```

On 27 states it has three nine-cycles. Steps 3 and 6 close a coarse receiver while retaining outer residuals 1 and 2; step 9 closes the full receiver. The retained result reports 729 state/time checks, exact inverse checks and nine canonical byte-content handoffs. Its rolling `(PAST,PRESENT,FUTURE)` policy is expressly a deferred scout and contributes zero scored evidence. The odometer's separate single 27-cycle is preserved as a distinguishing countermodel.

The curved pi donor also remains present: the recovered [curve account](accepted/sources/4e94911226_CUBES-AND-PI-CURVES.md) gives the Bishop equations `gamma'=T`, `T'=aU+bV`, `U'=-aT`, `V'=-bT`, with initial placement/frame and the two bend functions retained. Circle closure gives `kappa*L=2*pi`; position, tangent and full-frame closure are separate requested readouts. This note relies on the existing recovery for that source rather than claiming a new curve experiment or forcing it into the decimal/CRT carrier.

## 7. What still blocks the stronger requested transfer

The historical construction meaning can remain broader than any one donor. To compose them into the current task, each new required input or output must be supplied explicitly:

| Requested readout | Already recovered | Missing obligation / hostile distinction |
|---|---|---|
| Exact up/down navigation | Finite pi-cell involution; quotient-word next/previous action; lift/readback equation | A common source/operation adapter between these carriers. Uncertified next pi digit leaves ten refinements; a nonunit modular shift lacks the declared inverse. |
| Preserve the past across a jump | Source roots, frame action and retraction are retained in the donors | State whether the past means a receiver class, full state, raw displacement, or original occurrence/history. Audit84 actions recover one remainder but leave raw deltas MANY. |
| Continue through future handoffs | Direct FullZip bytes; checked landing; supplied rolling-triple law | A successor rule and proof of observation, enabledness and successor preservation for the actual future family. Correct present digits can merge states with distinct future behavior. |
| Secure past/present/future | Byte binding and failure-leaves-prior-current are stated mechanisms | Security property and actor/access model; an authority/freshness anchor and protections against allowed tampering or rollback if those are in scope. The master guide itself identifies coherent deletion beyond a visible head as needing an external head anchor. Hash identity and deterministic coordinate reconstruction do not establish confidentiality, authentication or forward secrecy. |
| YM2 transfer | Geometric incidence, exact inverse/handoff structure and action-aware sparse reconstruction are useful donors | An explicit map from admitted YM state/observable spaces to this rail state, together with transported dynamics/energy form and its inverse/fiber. The relevant measure, inner product/norm, gauge/equality convention and spectral/energy readout must travel through the map. |
| YM2 gap/coercivity conclusion | Closure can preserve a supplied law once its adapter is proved | A positive lower bound for the actual interacting quadratic/energy form on the intended vacuum-orthogonal domain, with every omitted fiber direction controlled; if a limit or scaling claim is requested, a bound uniform in that regime. A rail that erases an energetically relevant mode defeats the transfer even when its address and roundtrip identities pass. |

The security and YM rows are missing contracts identified by this recovery, not newly proved refutations of all possible uses. A candidate may supply them. Until it does, the security and YM completion fibers are OPEN; they are not NONE, and no complete ONE/MANY classification of those new problems is claimed here. No new finite control experiment can replace the missing transport/energy hypothesis by itself.

## 8. Source byte identities and verification

SHA-256 values below were calculated from the currently readable source files during this recovery. They bind those bytes only. Historical PASS statements remain attributed to the inspected records; the hash calculation does not replay or validate their contents.

Abbreviations are exact roots: `QR` is the Quantum Research root in section 1; `FOAM` is `C:/github/RPRM-foam-development`; `OPEN` is `C:/github/RPRM-open`; `PI` is `C:/github/RPRMResearch/RPRM_PI_THREE_LEVEL_345_MATRIX_01`.

| Source | SHA-256 |
|---|---|
| OPEN/recovered-concepts/CUBES-AND-PI-CURVES.md | `526AB16BA66ADDB1E6EBADE597A2470F44D83761705F29333E4EE439C5C9EF0E` |
| OPEN/recovered-concepts/RPRM-CONCEPT-RECOVERY.md | `5CD1318D303F89FD587692E7D9D5D87F96C285441FA6F530EA70A73047737829` |
| QR/RPRM_KERNEL_STILL_POINT_2026-08-12.md | `17E74561EAB6D90D3BAD876B607A77EC7E670D891D5C55DA034EE619AC784DEE` |
| QR/RPRM_BRIDGE_FAMILIES_FULL_REREAD_UNDERSTANDING_PACKET_2026-08-12.md | `D38EF532B2ACE34FB71114B4E29338ECD1BF466914424F862912BE2EB6BCE2C7` |
| QR/RPRM_MASTER_ASSISTANT_CONTEXT_GUIDE_WORKING_0_1_2026-08-13.md | `A939F252816B5E73686FCE64A3B19CAA39151637D56749CD3B83B84B6F19708C` |
| QR/experiments/RPRM_FULL_ZIP_HYPERCUBE_LADDER_01/RESULTS_SUMMARY.md | `EA9B6FA8F811D8A4BC78AE3E965783537D5CA459627B5855D3F5715329DE8E81` |
| QR/RPRM_TYPED_PLUS_ONE_RECURSIVE_PORT_SCOUT_0_1_SHADOW.md | `EB1807545B7D3E0F5812A4A16F25F00DF3F62B04976FF34FFD070FC331526EE2` |
| QR/experiments/RPRM_TERNARY_NESTED_HANDOFF_01/RESULTS_SUMMARY.md | `82A0AF5E270CA8B88515A0FF8AC8FD82527BF644B31DDB8A158A3E713113A081` |
| PI/README.md | `6BAB2B3BEF797D10D4609429BB6E4FE0053D97D7F1C857FDCE6A6B06DF90D59D` |
| QR/math-discoveries/outputs/RPRM_PI_THREE_LEVEL_345_MATRIX_01_EVIDENCE.md | `F53904F29820DB4B02A3D01943F1B49E40B7873D879276D95ECC95C4851AA853` |
| FOAM/experiments/RPRM_REMOTE_TEMPORAL_CRT_INTERIOR_PEEK_AUDIT_83/REPORT.md | `BD4FCD84A8FC26A11873CB33480C63EBA7941A486B55C95410FE36A3AE98EA82` |
| FOAM/experiments/RPRM_MULTIFRAME_THREE_LOSSY_WINDOW_AUDIT_84/REPORT.md | `28989CC615A869EAF0CAD809B1DA3B85A4D570CBA8CAD16D4BF7DE0F63627201` |
| C:/Users/bkbee/Downloads/Investigate RPRM bridge families CHATLOG.txt | `BE7B068F327CAFBA446F059A009A272ABB1A59FA085BC9AAA97B8BF4ADD995D3` |

Verification scope: targeted source reads, exact source existence, source hashes, and this note's equation/locator review. This note is the only file authored by the rail-recovery subtask.
