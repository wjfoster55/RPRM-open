# BSD E34 trace experiment02

Read [RETURN_TO_WILLIAM.md](RETURN_TO_WILLIAM.md) first. The new result is
**Sha(E34/Q)[5^infinity]=0**, for y²=x³−1156x, via an exact trace valuation
and the admitted published theorem. Full BSD remains open.

From this directory, run:

```text
python -I -B work/run_trace.py --output runs/my_fresh_run
```

Use Python3.10 or later, with assertions enabled (no `-O`). No packages,
network access, Sage, database or saved numerical certificate are needed.
The destination must not exist. The runner copies only Python source into
it, executes all six stages in isolated Python processes, and writes fresh
evidence and logs. Failure is preserved with an OPEN status. It takes about
9 seconds on the recorded machine. The included final replay is
`runs/return_validation/RUN.json`.

Source responsibilities:

| Source | What it computes |
|---|---|
| `work/cm_action.py` | Fresh point counts and the complete Gaussian action group |
| `work/trace125.py` | Certified field, primitive torsion lift and full orbit trace |
| `work/division_orbit.py` | Separate E' group law and character-weighted chord orbit |
| `work/trace_readback.py` | Independent ring multiplication, five coefficients and Newton trace |
| `work/trace_formula_reassembly.py` | Independent point/torsion and term reassembly |
| `work/run_trace.py` | Fresh source snapshot, execution logs and cross-modulus comparison |

The precise admission and receiver are in [CONTRACT.md](CONTRACT.md).
The main proof is [TRACE_CALCULATION.md](TRACE_CALCULATION.md); the
[action audit](TRACE_FORMULA_AUDIT.md), [alternative construction](DIVISION_ORBIT.md),
[YM transfer](YM_TO_BSD.md) and [rebrief](BSD_REBRIEF.md) supply their details.

`dependencies/` contains byte-preserved earlier written proofs and source
locators/hashes. Their relative links describe their original directories;
they are attributed historical dependencies, not newly replayed numerical
evidence. The new trace programs do not read those files or old PASS records.
The readback programs consume witnesses generated by the new trace and
recalculate their stated obligations, rather than accepting its status.

`MANIFEST.json` binds delivered bytes. Hashes are not proofs of mathematics.
The signed distinguished trace, total Sha and full BSD remain explicitly open.
