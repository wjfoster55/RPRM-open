# BSD trace 02: bounded continuation

Authorized task: inspect the completed Yang–Mills argument read-only for a
useful mathematical transfer, then perform the pending E34 calculation modulo
125. The inspected YM task was not modified, messaged or interrupted. This
directory is separate from its work and every earlier BSD delivery.

The fixed curve is E/Q: Y²=X³−1156X. The auxiliary model is
E'/Q: y²=x³+289x. The coefficient carrier is the exact finite ring
R_m=(Z/mZ)[z]/(z^16−2), m=25 or125, with i=57 modulo125.
Residue-field and point equality retain all sixteen coefficients; O is a
separate point. An elliptic-curve inverse negates Y. Gaussian multiplication
acts by [a+bi]P=[a]P+[b]([i]P), with [i](X,Y)=(−X,iY).

The requested receiver is the valuation at5 of the particular critical Hecke
value c5_plus(E), sufficient for a published 5-primary Sha criterion. It is
not the complex leading coefficient L''(E,1)/2. Unit multiplication preserves
this valuation receiver; it does not preserve a signed rational trace.

Supplied ports: the displayed models; the published CM/ray-class and critical
trace theorems; earlier written proofs that both ranks are2. The missing
port at entry was the correctly identified degree256 trace modulo125. This
experiment constructs a complete 512-point cover, proves its fiber size2,
and computes that readout. Division by2 is enabled because2 is a unit at5.
All other inverses are checked to have nonzero reduction modulo5.

Coverage: one curve, one odd prime, one finite trace. A primitive point must
pass all three Gaussian-prime annihilator tests, not merely have integer
order68. Enlarging the selected conjugate orbit to include its negatives is
a hostile case: the odd trace integrand then cancels, changing the receiver.

Evidence grades: written proofs using named published theorems; fresh exact
finite computations; separately attributed earlier rank proofs. No formal
proof assistant, database rank, expected decimal, or saved PASS record is an
input to the new trace. Search failure, a nonunit denominator, orbit failure,
or an inconclusive zero modulo125 would remain an open obligation.

At completion the trace valuation fiber is ONE(2), and the established
5-primary group is ONE(the trivial group). The signed distinguished trace,
unexamined prime-primary groups, total Sha and full complex BSD identity
remain OPEN. No complete fiber over those open questions is asserted.
