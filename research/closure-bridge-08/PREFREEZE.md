# Bridge 08 — RPRM-first candidate freeze

The user's current request asks for a semantic step back, forwards/backwards reasoning together, rise/fall inversion, and a proving-donut completion using the existing corpus. This pass supplies a bounded candidate implementation, not an assertion that research progress is objectively at digit seven or that the Millennium problems have only one small gap left.

Prior master: `../closure-master-07/README.md`. Research writes are confined to this new packet and its sibling delivery ZIP/receipt. Other task lanes remain read-only. No old campaign, paper or background automation is started.

## Candidate slate, before execution

1. **Mirrored motion:** on a lifted positional digit state, reflect the full state and test whether forward increment becomes backward decrement, including wraps and finite boundaries. Compare with reflecting only the digit.
2. **Conserved pair:** if one side rises while its partner falls, test which quantity is conserved and whether conservation alone forces a terminal event.
3. **Meeting by display:** test whether a forward prefix and a backward target constraint that agree only in their displayed digit can be joined into a lawful path.
4. **Meeting by joint state:** retain the same boundary state, split time, carrier and transition relation. Test the exact forward/backward path factorization. Keep existence distinct from a forced readout across every compatible completion.
5. **BSD missing interaction:** work backward from the exact rank-two target to determine what a candidate potential must depend on; test whether a sum of separate single-height contributions can obey the required mixed refinement law. This is a new candidate-class test, not a numerical coefficient rerun.

Finite carrier for tests 1–3: base b in 2,...,10, carry row q in -2,...,2, digit d in 0,...,b-1; lifted integer z=bq+d. Increment/decrement are partial at the declared outer boundary. Reflection is z -> b-1-z. The written parameterized law is stated separately.

For test 4 use all directed relations on three states, singleton starts/targets, horizons N=0,...,4, and every split k=0,...,N. Compare set propagation against complete path enumeration. These small models prove no general algorithmic speedup.

Named hostile seam: base ten, initial lifted value 7, target 2, horizon 5, split 2. Forward reaches 9; backward constraint reaches -1. Their displayed digit agrees while their carry rows differ. The control target 12 instead has a valid full-state seam at 9. Target values are supplied test inputs, not conclusions inferred from the desired result.

Coverage and source-generation are independent obligations. Working backward from an assumed target determines necessary constraints; it proves the target only if a source-generated object actually satisfies those constraints with the original readout preserved.

The final ledger will distinguish established scoped laws, exact finite evidence, refuted candidate classes, and the remaining OPEN bridge. The numeric stage labels may be used as an explicitly proposed mnemonic after their obligations are stated; they will not choose the outcome of the audit.
