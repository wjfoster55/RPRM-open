# Independent mathematical review

12 September 2026. Reviewed `BIDIRECTIONAL_BRIDGE.md`, `agents/BSD_SEAM.md`, their three new checker scripts, and the inherited `research/bsd-identity-01/EXPLICIT_IDENTITY_TARGET.md`. No old experiment was rerun and no reviewed file was edited. Source locators below refer to the version read before root's review corrections.

## Finding to correct

**[P2] Separate storage is not additive separability.** [BSD_SEAM.md:10](C:/github/RPRM-open/research/closure-bridge-08/agents/BSD_SEAM.md:10) says a bridge that “separately retains three point corrections” cannot reproduce the mixed difference. This is stronger than the proved result: independently stored p, q and s suffice to evaluate `pq-(s-p-q)^2/4` using a joint decoder. The obstruction rejects an **additively separable potential** `F(m)+G_P(i)+G_Q(j)+G_S(ell)` on the asynchronous carrier, not separate storage or a tuple of source records. The later section states the correct narrower claim. Suggested opening: “An additively separable sum of one-point potentials cannot reproduce that difference on the declared refinement carrier.” This also makes clear that pair products are a required readout dependence, not necessarily new independent source information.

## Small clarity fixes

- [BIDIRECTIONAL_BRIDGE.md:52](C:/github/RPRM-open/research/closure-bridge-08/BIDIRECTIONAL_BRIDGE.md:52): the displayed base-ten path reaches carry rows ±1, so explicitly require `Q>=1`. The general carrier permits `Q=0`, where the path stops at 9 and its reflected partner stops at 0. The later carry-seam example already specifies Q=2.
- [BIDIRECTIONAL_BRIDGE.md:83](C:/github/RPRM-open/research/closure-bridge-08/BIDIRECTIONAL_BRIDGE.md:83): triples `(i,x,g)` give the exact endpoint/middle join, but do not retain all path multiplicity. Two distinct prefixes with the same i,x collapse in the relation P. The preceding statement about the complete path family is correct **when both full prefix and suffix witnesses are retained**. Add that the triple is a projection of this witness family, or explicitly retain `(prefix_path,suffix_path)` joined at x.
- [BSD_SEAM.md:287](C:/github/RPRM-open/research/closure-bridge-08/agents/BSD_SEAM.md:287): `-4 log H_0` is the unscaled local duplication correction δ, inherited from the source note; the first refinement increment is δ/4 = `-log H_0`. The mathematics is correct under that distinction, but this note should state it because its earlier “increment” is normalized. Suggested text: “its first unscaled duplication correction is δ₀ = −4 log H₀, so its first normalized height increment is −log H₀.”

## Requested checks: conclusions

1. **Seam existence versus a universal target — sound.** For the fixed Markov-complete state and relation, `F_k∩B_(N-k)` is exactly the set of kth states of length-N successful paths. Prefix/suffix concatenation proves the iff. The text correctly says this is existential and does not show an unknown source or every path satisfies G. The zero-horizon and empty-family statements are correct. Singleton-start/target exhaustive tests do not separately test all subsets, but union decomposition plus the written argument covers them.

2. **Reflection and finite boundaries — sound.** The integer interval represented by X is `[−bQ, bQ+b−1]`. Reflection `z↦b−1−z` exchanges its endpoints, is involutive, and carries `z<upper` to `R(z)>lower`. Thus `RUR=D` holds with the correct partial domains for every Q≥0. The only missing qualifier is the displayed path's Q≥1 noted above. An imposed finite boundary does not establish a domain-derived terminal property; the document explicitly preserves this limit.

3. **Paired-budget stopping — no off-by-one error.** After n completed transfers, `r_n≤r_0−n epsilon` gives `n≤floor(r_0/epsilon)`. Under nonterminal progress, terminal status must occur after at most that many transfers, so the number of visited states is at most `floor(r_0/epsilon)+1`. The argument also covers r₀=0 and 0<r₀<epsilon: no nonterminal state can be admitted there if it must enable a nonnegative-budget next transfer. The checker correctly calls `remaining < cost` terminal; it does not require an exactly zero remainder. Terminal correctness and nonreplenishment are separate explicit premises.

4. **Mixed difference and normalization — sound.** Expanding R gives cross coefficients +1/2 and square coefficients −1/4. Hence distinct-slot mixed difference is uv/2, same-slot formal mixed difference −uv/2, and the defect's P/Q mixed difference is `−Omega*sigma*uv/2`. With actual increments `(1/4)log(21025/16)` and `(1/4)log(124609/65536)`, the coefficient is correctly `−Omega*sigma/32`. The first-source rational duplications and removed gcds 64 and 16 agree with the displayed formulas. Zero factors are retained rather than divided out. Formal same-slot identities are polynomial controls; they need not be four actual states in one refinement sequence.

5. **Locked diagonal — properly excluded.** A one-dimensional locked path has no independent P/Q rectangle. Arbitrary functions of its one parameter can absorb interactions that a separable asynchronous potential cannot. The document explicitly enlarges the carrier, limits the obstruction to that enlargement, and states that a diagonal-only candidate is not refuted. Preserve these qualifications in the final report.

6. **Backward tails and boundary compatibility — sound at the conditional scope.** The asynchronous height bound follows from the product error and `|(b+delta)^2-b^2|≤2|b|t+t²`. For i=j=ell it reduces to the inherited bound's quadratic coefficient 13/4. The analytic tail plus positive Omega*sigma multiple is correctly retained. Finite intervals containing zero do not prove zero. On the connected refinement grid, matching every source edge gives precisely `V=D+c`; the initial boundary fixes c, and the limiting zero boundary remains the original real-identity obligation. No additional analytic or Sha conclusion follows.

The arithmetic and proof structure are suitable for the stated bounded bridge after narrowing the opening storage claim. The inherited analytic/rank/basis/period results remain attributed dependencies, not newly re-proved by these scripts.
