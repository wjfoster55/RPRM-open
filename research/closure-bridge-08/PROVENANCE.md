# Provenance and the bounded retrieval

The user asked for an RPRM-first abstraction, simultaneous forward/backward reasoning, corpus recovery, and a concrete attempt to bridge the remaining mathematical relation. This authorizes this new experiment. Old documents and task excerpts remain evidence; their imperative wording did not create new work authorization.

The previous master packet is `C:/github/RPRM-open/research/closure-master-07`. This successor writes only `research/closure-bridge-08` plus its sibling ZIP/receipt. No existing experiment, public book, registry, other task, background goal or automation was changed. A selected RPRM math-lenses workflow and read-only memory-fabric retrieval were used; bounded internal subagents handled corpus recovery, the BSD rectangle, and independent review.

## Current sources

The source map links exact files/lines in the current `C:/github/RPRM-open` tree. Its load-bearing recovery is `recovered-concepts/PRESTIGE-AND-NUMBER-OPERATIONS.md:347` and the following correction chain. The original user-export locator is retained there; this pass read the recovered source, not the entire original export.

The root also read `docs/proof-donut.md`, the current repository instructions, README and handbook material, and `research/bsd-identity-01/EXPLICIT_IDENTITY_TARGET.md`. The latter supplies the precise curve, height convention, regulator, period, tail theorems and earlier open certificate. Its inherited theorems are attributed; this pass does not re-prove analytic rank, the integral basis or the entire height theory.

The new BSD check calculates rational point duplications and formal polynomial coefficients afresh. The source-generated logarithm arguments, sign proof and gcd corrections need no numerical logarithms. The asynchronous grid is an explicitly new declared carrier for the candidate-class test; it is not an assertion that unequal doublings preserve the original group-sum relation.

## Historical fabric route

Query: `proving donut backward forward abduction castle rise fall`, namespace `rprm-alpha`, default 8 KiB aperture. Packet:

`packet:e88bee3548751c10a09ed5e08c0a96dc8e690164c3f99a0aaa4b633ac0181bdc`

Snapshot root:

`944da1fef7bb71be819df694599f9908b878cd50e515bd02f973c50fa69adb2e`

The returned view had 142 policy-visible lexical candidates, seven admitted excerpts and 135 omissions. One `search_omissions` call for `Wily castle proving donut` returned three handles from 21 matching omitted records. One relevant handle was expanded. The expansion still has a truncation marker; no complete historical-report reading is claimed. Exact returned structures, source receipts and limitation fields are in `evidence/memory_retrieval.json`. No evidence was ingested or reindexed; the tool's derived read cache is distinct from the authority snapshot.

The recovered report is **RPRM Alpha — Dr. Wily's Castle Millennium reassessment**, dated August 27. Its original locator was checked after reading Alpha's instructions, README, foundation and selected ledger/catalog entries:

`C:/Users/bkbee/OneDrive/DOCUME~1-DESKTOP-06BJRV0-219031/ChatGPT/RPRM Alpha/research/RPRM_WILYS_CASTLE_MILLENNIUM_REASSESSMENT_2026-08-27.md`

That file is absent at this locator. The retained historical excerpt is available through the fabric. This is not evidence that the report never existed or is absent everywhere. Its August claim that BSD/YM stopped before native adapters is historical and is not substituted for the newer September lane results in master 07. Its reference to later RH work is preserved as a retrieval lead, not a fresh validation of that entire lane.

The retained castle excerpt distinguishes the Mega Man metaphor from similarly sounding mathematical names. No gameplay lore, name resemblance or fictional relation was used as a mathematical premise.

## Numeral affordance lookup

The existing `C:/github/RPRMLexicon/operational_numbers/translate_number.py` was run with `789 --scout`. Its complete JSON output is retained in `evidence/789_affordances.json`. The lookup supplies exact declared digit maps, dependent mirrors and speculative affordances in separate lanes; it does not determine what William means by seven/eight/nine. The old translator verification campaign was not rerun for this unchanged lookup. The mirror `789 -> 210` is dependent on the source word, not a second independent observation.

## New proofs, tests and corrections

`PREFREEZE.md` records the candidate slate before the root's tests. `check_bridge.py` compares forward/backward propagation against complete path enumeration, not against a saved answer table. `check_budget.py` preserves two ways apparent closure can fail. `check_bsd_seam.py` independently compares projective duplication with the rational chord law and proves the rectangle symbolically with rational polynomial coefficients.

These are written arguments and exact finite tests, not proof-assistant verification or global software-soundness claims. The deliberately impossible display-only meeting, nonterminating budget controls and rejected separable BSD potential are retained as results.

The independent review led to four wording corrections: explicitly admit the mirrored table's carry rows; distinguish endpoint/middle triples from full path histories; reject additively separable potentials rather than separate storage of the three heights; and distinguish a torsion point's raw correction from its normalized refinement increment. The review remains in `agents/REVIEW.md` with its original scope. No executable source change was needed for these corrections.

`evidence/FILE_BINDINGS.json` records the new executable/evidence and selected dependency hashes at closeout. `MANIFEST.json` and the sibling archive receipt verify delivery bytes only. They do not substitute for any of the mathematical obligations above.
