# Sliding rings and tentative digit operations

The user requests bringing the three hoops closer together, sliding and
viewing them freely while linked, and scouting readings of 592595 and
7394 with 2 as a double/hinge and 9 as a seam. Preserve this as an
expanded motion model and a tentative digit grammar.

S1. Use radial meridian hoops on a fixed connector, with angles phi-a,
phi,phi+a, where 12<=a<=120 degrees and -180<=phi<=180 degrees. Viewpoint
is a separate rigid rotation. Prove disjointness continuously, including
rendered tube clearance. At a=0 the identical hoops collide and are not
admitted. Previous projected-hub uniqueness used a particular separation;
do not reuse that conclusion without its hypotheses.

S2. Freeze the post-target candidate F(w)=maximal suffix-prefix overlap
of w and reverse(w), for exactly two decimal digit occurrences. Test
59->595, 55->55 and all 100 width-two words. A candidate record
w | 2 | F(w) yields 592595 at w=59; the 2 is a declared two-view tag,
not an arithmetic multiplier. Compare literal doubling and duplication.

S3. Trace the source digits through the earlier D enclosure, retaining
the negative lower prefix 50, the decimal scale and both endpoints.
Test a value-preserving change of display and a safe widening. These
tests distinguish a literal bound-word pattern from a fixed BSD value.

S4. Construct exact hypothetical pairs inside the already supplied A,B1
intervals, one with D=0 and one with D=1/10000. These demonstrate the
information absent from the interval-plus-drawing premises. They are not
claimed as alternative values of the actual L-function or regulator.

No new height or analytic-coefficient evaluation, old-lane rewrite,
shared-registry update or paper is authorized by this candidate slate.
The requested new conclusions are bounded geometry, exact named digit
operations, and an understandable statement of the remaining obligation.

S5 (added before its own tests). Interpret the retained path a|b|a as
two rational ratio legs a/b and b/a on nonzero decimal digits. Test exact
product one and its complete 81-input fiber. Keep all zero-digit exclusions
and distinguish an identity roundtrip from equality a=b. This is another
post-target interpretation, not a claim the numeral source used ratios.
