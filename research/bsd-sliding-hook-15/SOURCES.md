# Sources and execution scope

1. The user's current proposal supplies the requested sliding motion and
   the tentative readings of 592595, 7394, 2, 9, and 55. It does not supply
   a formal grammar; the new candidate definitions are explicitly ours.
2. `research/bsd-coefficient-05/TWO_SCALES.md` supplies the attributed
   intervals A=[6.38511803,6.38518585] and
   B1=[6.384593255,6.385625424], and the normalization for E34.
   These source values are recorded in the exact-arithmetic evidence.
   This experiment checks their subtraction and uses them as premises;
   it does not reprove their original analytic or height certification.
3. `research/bsd-keyring-13/agents/KEYRING_AUDIT.md` supplies the original
   labelled keyring family and linking calculation. The new geometry
   audit writes out the sliding family, separation proof, spanning disks,
   and explicit ambient isotopy.
4. `research/bsd-keyring-view-14/README.md` supplies the preceding fixed
   embedding's projection claim and the discrepancy interval. Its spacing
   hypotheses are preserved; the new clustered counterview prevents
   extending the visual restriction to the expanded family.
5. `evidence/translator-scouts.json` retains all six actual scout outputs.
   `evidence/translator-verification.json` records the narrow translator
   verification (114/114) and its source bindings. These outputs are
   candidate discovery evidence, not authority for new numeral meanings.
6. `evidence/display-bounds.json`, `evidence/digit-checks.json`, and
   `evidence/ratio-loop-v2.json` are fresh standard-library executions for
   this packet. The initial ratio script and receipt remain as
   `work/check_ratio_loop_initial.py` and `evidence/ratio-loop.json`.
   Independent review requested explicit interval-membership assertions;
   the current script adds them and v2 is its fresh execution.
7. `evidence/visual-check.json` records Playwright interaction and layout
   checks of `evidence/sliding-preview.html`. Three PNGs retain desktop,
   edge-on, and mobile views. All three were visually inspected.

The earlier source paths are relative to C:/github/RPRM-open and remain
unchanged. Their equations are attributed inputs, not new verification
of prior proof claims. No external research task was changed or messaged.
