# A tested digit candidate, with its source and grammar retained

The proposed `59 -> 595` reading has an exact realization: reverse `59` to
`95` and identify the shared `9`. With **maximal suffix-prefix overlap**,
`59 ⊙ 95 = 595`, while `55 ⊙ 55 = 55`. The candidate supplies a precise
hinge and a fixed-word version of “stop.” It is **POSTTARGET fitted**:
the observed endpoint helped select this grammar and the newly declared
`2` opcode. Its successful execution does not establish that the numerical
enclosure was generated by this rule.

## Preserve the input before interpreting its digits

The [prior enclosure](../../bsd-keyring-view-14/README.md) is
`−0.000507394 <= D1 <= +0.000592595`. The corresponding signed rational
endpoints are `−507394/10^9` and `+592595/10^9`. Selecting the upper word
`592595` retains the scale and sign in this source record. Selecting the
lower suffix `7394` additionally sets aside its prefix `50`:
`507394 = 50|7394` as a word. Neither prefix, sign, nor scale disappears
from the originating bound. These are interval endpoints, not measured
values of D1; the interval includes both zero and nonzero values.

## Exact declared rule and complete finite fibers

Let `Σ={0,...,9}` and `W=Σ²`, the 100 **ordered two-digit words**, including
`00` and `05`. Equality is word equality with width retained. Reversal
`R(ab)=ba` retains two separately tagged input-view occurrences, even when
their word values agree. For words u,v, define `u ⊙ v` by concatenating
after the **longest** suffix of u equal to a prefix of v; full overlap is
admitted. The chosen operation is `F(w)=w ⊙ R(w)`.

For `w=ab`, one-symbol overlap always exists because the shared symbol is
b. Two-symbol overlap exists exactly when `a=b`. These exhaust the overlap
lengths. Consequently:

```
F(ab) = aba  if a != b
F(aa) = aa
```

The supplied port is w; the reverse view and output are computed dependent
ports. The receiver retains the oriented source word and the derivation
tags. The retraction `first_two(F(w))=w` proves injectivity. For every
admitted output word z in `Σ² ∪ Σ³`, its complete preimage is `ONE(z[:2])`
if `F(z[:2])=z`, and `NONE` otherwise. Exhausting all 1,100 output words
gives 100 singleton fibers and 1,000 empty fibers. Other lengths or
nondecimal symbols are admission errors for this target carrier.

Declare a separate certificate serialization
`C(w)=w | 2 | F(w)`, where the middle field is a literal **two-view opcode**.
This is a new convention, not arithmetic multiplication or an intrinsic
meaning of digit 2. Removing the displayed separators gives:

| Source | Reverse view | Overlap output | Certificate word |
|---|---|---|---|
| `59` | `95` | `595` | `592595` |
| `58` | `85` | `585` | `582585` |
| `55` | `55` | `55` | `55255` |
| `05` | `50` | `050` | `052050` |

On the declared target `Σ⁵ ∪ Σ⁶`, the complete certificate fiber is
`ONE(z[:2])` exactly when `C(z[:2])=z`, and `NONE` otherwise. This follows
because every certificate starts with its two-digit source. Thus
`C⁻¹(592595)=ONE(59)`; `C⁻¹(592594)=NONE`. All 100 source round trips pass;
10 certificates have length five and 90 length six. The tags in the
typed record preserve both view occurrences; the naked output string
alone does not contain arbitrary source-occurrence identities.

F maps W into `Σ² ∪ Σ³`, so it is not a declared repeatable machine on W.
Here “stop” means `F(55)=55`, not a proved temporal halt. All ten equal-digit
words have this property, so `55` is not unique. The rule's shared symbol
is b: `58` has hinge `8`. No special universal role for `9` follows.

## The rival grammar and the lower suffix remain visible

Freeze the small grammar carrier `H={maximal_overlap, one_symbol_overlap}`.
Both give `59 -> 595` and `58 -> 585`, so the complete grammar fiber after
those observations is `MANY(H)`. They agree on all 90 unequal-digit words.
They differ on every equal-digit word: the second rule gives `55 -> 555`.
An independently adopted `55 -> 55` constraint selects maximal overlap
within H. William's suggested stop reading is not recorded here as that
independent acceptance. The wider space of possible grammars remains OPEN.
Ordinary doubling gives `2*59=118`; literal duplication gives `5959`.

A separate newly declared positional parser can read `t|a|9|b` as the
retained tag t and ordered pair `(a,b)`. On its 1,000-word carrier it
round-trips exactly, giving `7394 -> (tag 7, pair (3,4))`. Here `9` is a
delimiter by convention. Forgetting t gives the complete ten-word fiber
`{t394 : t in Σ}` over pair `(3,4)`. Reading the pair as the rational `a/b`
is a further adapter, enabled only for nonzero b. In its 900-word carrier,
`3/4` has the complete 20-word fiber `{t394,t698 : t in Σ}`. Even with tag
`7` retained, the rational readout has two sources, `7394` and `7698`.
The ordinary integer `7394` is not the rational `3/4`.

This parser supplies no transition or enabledness law for “7 = wait,”
and does not identify the pair with physical sides. “9 = divisor” is
distinct from this delimiter role: 9 divides none of `592595`, `507394`,
`7394`, `59`, `595`, or `55`. Additional meanings remain open candidates.

## Scout and execution evidence

All six requested translator commands ran with `--scout`; their full
JSON reports are [retained here](../evidence/translator-scouts.json).
`592595`, `59`, `595`, and `55` produce no sequence scout proposals.
The `507394` and `7394` reports recognize adjacency `39` and propose a
root-square probe supported by `3²=9`, `N(3)=7`, and `P(3)=3` in their
declared maps. These are concurrent affordances; they select no meaning.
The translator does not supply the overlap rule or the new opcode/parser.

Only the operational translator verifier was rerun because no current
byte-bound receipt was available: **114/114 checks passed**; executable,
lexer, reading-data, and verifier hashes were unchanged across execution.
The [receipt](../evidence/translator-verification.json) binds those bytes,
without claiming general soundness. No full research campaign was run.

The [finite check output](../evidence/digit-checks.json) includes every
width-two row, grammar distinctions, inverse fibers, lower-parser fibers,
and exact subtraction of the attributed interval inputs. Replay locally:

```powershell
python -I -B C:\github\RPRM-open\research\bsd-sliding-hook-15\evidence\check_digit_scout.py --output C:\github\RPRM-open\research\bsd-sliding-hook-15\evidence\digit-checks.json
```

Evidence grades: the operations and grammars are declared definitions;
the case split and retractions above are written finite proofs; the
attached execution is exhaustive finite testing. No universal digit
semantics, common BSD invariant, or conclusion `D1=0` is established.
