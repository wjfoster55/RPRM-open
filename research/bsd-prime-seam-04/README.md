# BSD prime-seam experiment 04

William asked whether a meeting of prime paths, lucky numbers or related
operational patterns could reveal the missing multiplier. This bounded
continuation found exact shared Gaussian factors in the actual local
comparison formula. The global BSD scalar remains OPEN.

Read `RETURN_TO_WILLIAM.md` for the explanation, `PRIME_SEAM_PROOF.md` for
the algebra and exceptions, and `MULTIPLIER_FRONTIER.md` for the exact
remaining obligation. `RPRM_SOURCE_RECOVERY.md` records the source lens and
which proposed transports survived. This is an experiment, not a paper.

Run from any directory with Python 3.10 or later; no packages are needed:

```powershell
python -I -B C:/github/RPRM-open/research/bsd-prime-seam-04/work/run_seam.py --output C:/github/RPRM-open/research/bsd-prime-seam-04/runs/my_fresh_replay
```

The output directory must be new. After unpacking the return ZIP elsewhere,
replace the path by its unpacked experiment path. The runner snapshots all
source before computing the prime census, original-formula readback and
scale obstruction. It uses no saved computational PASS record as input.
The old real interval appears only as an explicitly admitted constraint
for the countermodel; it is not re-certified in this experiment.

Fresh deliverable evidence is under `runs/final_validation/`. Initial
exploratory evidence and its first source version are retained separately.
Source hashes identify bytes and do not prove their content. Verification
combines exact computation, written elementary proofs and explicitly named
theorem dependencies; it is not proof-assistant formalization.
