# Carrier and operation contract

The user authorized a bounded BSD continuation looking for multiplier
connections. Recovered documents and historical imperatives are attributed
evidence. Only this new BSD experiment and its return archive are written.
Other research lanes, old experiments, memory authority and papers are
outside this mutation scope.

The curve is E34/Q: y²=x³−1156x. The following carriers stay distinct:

| Carrier | Equality / operation | Requested receiver / boundary |
|---|---|---|
| Q(i), rational Gaussian coordinates | exact pairs; multiplication, conjugation, nonzero division | local factors and their complete inverse relations |
| Q5 and Q13 | fixed embeddings of Q(i), then declared finite precision | valuations and residue readback; finite digits do not specify arbitrary global scalars |
| E(Fp), good odd primes | full point coordinates plus O | point counts and a concrete order-five witness at 13 |
| Integers 1..1000 | prime predicate and positional lucky sieve | complete pointwise joint codes; counts forget positions |
| Words in four CM generators modulo 68 | residue action and separately assigned exact product of C factors | equal action need not mean equal product; this is a proposed weighted adapter, not an asserted Kato norm law |
| Real and p-adic BSD coefficients | normalized analytic/arithmetic tensors as in BKS | the exact common multiplier is still OPEN |

The scout's good ordinary primes satisfy p≡1 mod4 and p∉{2,17}. The
algebraic root representative a+bi has b>0. Its use as the unit root at a
particular p requires the specified embedding; at 5 use i≡2, at 13 use i≡5.
Conjugation reverses that choice. Complex norm one is not p-adic valuation
zero. The root labels are not silently interchangeable in the BKS theorem.

Given C5 and C13, division uniquely determines their quotient: ONE(value).
Given only their quotient D, the complete nonzero pair fiber is
MANY({(u,Du):u∈Q(i)^×}); if both factors must have norm one, restrict u to
the norm-one subgroup. The curve and chosen roots supply each local factor
separately; the quotient does not supply a global analytic coefficient.

Given v5(Cp), multiplying by C5 to that integer power removes the oriented
5-part of this known Gaussian scalar. One original factor and the exponent
make this invertible. Omitting the exponent forgets the removed part.

The prime/lucky joint receiver has a complete finite solved fiber at every
address. It does not admit a Frobenius operator for a composite lucky value.
At a prime shared by the two lists, the existing curve determines Frobenius;
the lucky bit is an additional predicate whose scalar discrimination is
tested rather than assumed.

Unfinished global comparisons are OPEN. The exact failed implications and
their distinguishing examples remain in the mathematical notes. No failed
global obligation is replaced by a successful finite software check.
