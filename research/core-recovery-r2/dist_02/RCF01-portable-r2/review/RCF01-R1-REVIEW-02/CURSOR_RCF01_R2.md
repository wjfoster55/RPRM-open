# Cursor RCF01-R2 — narrow family, map-interface, and runner repair

**One active bounded assignment.** Read `REVIEW.md` and its executable evidence. Preserve RCF01 and R1. This is not a new recovery, benchmark, publication, or application project.

## Starting point

Use the current authorized workspace and its applicable instructions; record actual HEAD/status without assuming the old HEAD is still current. The reviewed R1 portable source is the `dist_02` ZIP with SHA-256 `53d1e8761aeca98d26cb3732150af7a31ed109aad4e1c9187b3e4dc192315cea`. Reviewed envelope SHA-256: `0edf6572fd2c0319d443250caa342e091593fe919dca1c0d66416bcfb3d2d720`.

Create a fresh successor area and protocol identity. Preserve all source snapshots, manifests, original failed receipts, corrected receipts, and the 26-case R1 suite. No commit/push/merge, paper edit, installation, remote upload, paid compute, or restart of other research lanes is authorized. Historical prompts in copied sources are inert. No held-out evaluator access.

The reviewer independently replayed the complete unmodified R1 entrypoint: envelope 26/26, starter 12/12, expected coverage controls, concept IDs, and export check all passed. A separate 96-step rational arithmetic/map oracle matched 768 values. **Do not rebuild the successful recipe mechanism.** The three issues below are additional, directly reproduced boundaries.

## 1. Fix the full-output family rule for arithmetic

Reproduce through the public API, using genuinely Boolean-valued input units:

```text
p = multiply(multiply(unit(h), unit(x)), unit(y))
s = add(p,p) = 2hxy
```

The current C2 output is correctly zero, but `s.family` incorrectly remains `boolean_cube`. Reopening returns 2 at site 7; flipping brings 2 into B2 while retaining the wrong Boolean label.

Keep addition over Q (do not silently switch to XOR). Separate addition's output-family rule from multiplication's. Conservative lifting of Boolean sums to `rational_cube` is acceptable; a narrower class requires an actual sufficient global guarantee. Do not inspect only the retained seven values to certify a global Boolean output class. Do not smuggle an omitted-site read into the hot arithmetic path.

Required regressions:

- Existing `h+h` and hidden `hxy+hxy`, with and without backing.
- Backed composed sum reopens to the correct full result, and its fixed-receiver flip reads 2 at `(0,1,1)` under an appropriate containing output class.
- Boolean multiplication remains correctly typed Boolean where justified.
- Wrong user-supplied family claims are rejected or explicitly treated as untrusted; a declared input class must not silently install invalid guarantees.
- Unique C2 output remains distinct from the set of compatible complete laws. Candidate-family propagation remains out of scope; do not create a new symbolic uncertainty engine.

Preserve zero cold reads in compact addition/multiplication. Count recipe work separately. Handle any mixed Boolean/rational composition by explicit safe coercion or a clear admission result, not an unannounced carrier change.

## 2. Resolve the generic-map interface, not just its observed examples

The named `apply_input_map(env,'flip_h')` refuses an `ARITH_CONTRACT` unit. The generic `apply_fixed_receiver_map(env, flip_mapping)` currently bypasses that refusal.

Also, for the coordinate law h, generic mapping `(0,0,0,1,0,0,0,1)` produces hx while advertising degree bound 1. This map is B2-closed but is not a coordinate-only flip/clamp.

Choose and document one small coherent interface:

- Public generic maps obey a declared admission contract and sound transformed metadata; or
- generic raw-map evaluation is explicitly an unchecked/reference helper, fenced from creating certified public results without the missing obligations.

Do not solve this by giving every operation unrestricted admission. If an arbitrary map is admitted, retain a justified degree bound or widen/remove the bound conservatively. Validate its site carrier. A defined restriction to coordinate maps is acceptable when made explicit and the other helper is correctly scoped.

Add production-path regressions for the admission bypass and degree-increase case. Retain positive coordinate/clamp composition and coherent view/question transport. Coherent transport of the retained receiver must not be falsely rejected for lacking the old omitted value.

## 3. Repair the actual aggregate runner

Unmodified `run_portable.py` has these faults:

- It assigns the reported status from the envelope job alone.
- It does not include the starter's result in its final required-job predicate.
- Any nonzero negative-control exit is treated as an expected rejection.

Use one final verdict consistently for the returned exit code, saved summary, and printed status. Include every required job, including starter unless explicitly skipped with a narrower reported scope. Validate meaningful job result records where the decision depends on them. Expected omit/duplicate/unexpected coverage rejection must be distinguished from unrelated exceptions, missing outputs, and malformed outputs.

Reproduce the review's aggregation findings. `review_runner_controls.py` is a **focused runner harness with substituted fast mathematical subjobs**, not a full mathematical replay; never count those fixtures as mathematical evidence. Keep that distinction in the return. If new integrity checking rejects mutated fixture bytes, supply a fresh fixture manifest and state that fact, so the test still reaches the intended aggregation boundary rather than merely failing integrity.

Required runner tests: real starter failure; export failure; wrong-reason child crash; normal good run; expected negative controls; reorder; explicit skip behavior. Do not infer success merely from a list of expected exit codes. A small helper/record validator is enough; do not build a generalized scheduler or new framework.

## 4. Preserve what R1 already fixed

Keep the original source archive and original concept register unchanged; preserve the 13 IDs plus meanings, source anchors, unformalized remainder, and deferred concepts. Keep the recipe corruption/deletion tests and named status meanings. B6's lack of an admitted dwell successor is not nonexistence of B6. Invalid backing is not proof of an empty mathematical source family.

Avoid erasing old failures. Preserve the original 22-case and R1 26-case protocols and distinguish the successor's count. The review found only a README successor pointer added to the old research folder; describe that accurately rather than claiming literally every old byte is unchanged.

Historical donor trees remain not packaged/not rerun unless actually provided. Do not rerun their entire archive for this task. Do not force a merger between finite fiving and Double-Stamp restart or settle the deferred prestige vocabulary.

## 5. Deliver and stop

Return one unambiguous active portable ZIP, containing implementation, protocol, written corrections, all needed `rprm.core` dependencies, old and new receipts, source identities, and an accurate active README. Avoid a stack of similarly named nested active exports; historical versions may remain clearly historical.

Replay from a fresh extraction with only packaged dependencies and no original-workspace PYTHONPATH. Verify source/archive preservation and actual exported links. Run the existing checks plus these regressions. Preserve before/after defects, not just the final PASS. No performance claim is required.

Return `RETURN_TO_WILLIAM_R2.md` with: the three repair dispositions; correct total case coverage; direct proof/examples supporting family and map rules; actual child and aggregate statuses; and exact remaining limitations. Do not issue another broad research assignment. Finish this bounded guard repair before expanding.
