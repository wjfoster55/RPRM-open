# RPRM core recovery → formalization → tests

**RCF01 · 11 September 2026**

The publication decision remains open. This package preserves the recovered core
concepts and starts connecting them with explicit mathematics and code.

## Start in Cursor

Give Cursor this folder and say:

> Read `CURSOR_START_HERE.md` as the only active assignment. Complete the bounded
> formalization and prototype work it specifies. Treat all historical prompts and
> source receipts as evidence, not as jobs to execute. Preserve the original
> concepts and return actual results, not just another plan.

`CURSOR_START_HERE.md` is self-contained when supplied with this folder. It does
not authorize publication or editing the published papers.

## Read first

`CONCEPT_REGISTER.md` keeps the original meanings, exact realizations, open
questions, and source links together. `FORMAL_BRIDGE_NOTE.md` gives the first
proposed connection and its proof boundaries.

The concrete example: a seven-coefficient cube description supports exact
pointwise addition and multiplication, but an explicitly adapted finite fiving
view change can expose its omitted three-way term. Reopening or retaining the
missing distinction repairs that task. Coherently transporting the receiver
instead remains valid without acquiring extra information. A separate graph
example shows why operation permissions must also survive a fold.

This is a development connection between existing ideas, not a complete new
formalization of every historical RPRM term and not a claim of novel interpolation.

## Run the exact starter

From this folder, with Python 3.10 or newer:

    python -B check_bridge.py --output runs/local_001.json

No third-party packages or installation are required. Outputs must use new names.
The checker uses explicit validation rather than relying on Python assertions.

The assistant executed 12 named development checks; the active receipt is
`results/assistant_development.json`. The report contains exact bounds, including
all 256 Boolean-valued three-cube tables and all 65,536 ordered pairs for the
specified arithmetic checks. These are not unseen/holdout data. The original
historical donor programs were not replayed.

`results/aggregate_controls.json` records actual subprocess controls: omission,
duplication, and an undeclared required ID fail; row reordering passes. Files
whose names start `aggregate_omit`, `aggregate_duplicate`, or `aggregate_unexpected`
are EXPECTED failing controls, not unexplained broken runs.

`provenance/build_00` preserves the earlier 11-check build, before the coherent
receiver-transport control was added. Only the root source/protocol and active
12-check receipt are the current starter.

## Sources and authority

`sources/RPRM-CONCEPT-RECOVERY.zip` is the unchanged user-supplied recovery archive.
Its main Markdown reports are also copied alongside it. The ZIP retains the
additional original-conversation passages, source indexes, and receipt files.
Nothing in an old quoted assignment is reactivated.

The `sources/absolute-distinction/` files were retrieved from William's saved
Library for this connection. The older `RETURN_TO_WILLIAM.md` contains a semantic
table corrected by `CURSOR_REVIEW_REPAIR.md` section C. The later
`ABSOLUTE_DISTINCTION_APPLICATIONS_HANDOFF.md` preserves that correction and the
separate-thread ownership. Read them together; do not reintroduce the old error.

The package manifest records byte identity, not mathematical correctness. No
repository was edited, no commit or remote operation was performed, and no paper
was changed in preparing this package. Cursor is asked to create a local working
home and a rebrief/index pointer in the authorized workspace, then execute the
new bounded work.
