# F1 closeout

**Complete with explicit evidence limitations.** Both requested source pins and
their saved result files were acquired. This is a closeout of three existing
studies, not a new benchmark campaign. F1_CLAIM_LEDGER.md supplies the claim-level
record and exact source identities; review/broadphase_review.md gives the bounded
second-reader audit.

The dynamic study's useful conclusion survives fresh execution: equal total volume
and container geometry do not determine the declared 300-frame breach bit.
The tall/flat pair gives 1/0, peaks 22/0, first tall breach 11. Both velocity arrays
start at zero. Tall is filled top-first, above the crest. The pinned model has no
upward water move, so its prose about running up the divider is not the actual
implemented mechanism. The full admitted deterministic simulator state suffices
for replay, but neither its global minimality nor a velocity-only repair follows.
The 84/144 stress result remains source-reported, with arithmetic checked.

The broadphase study preserves 51,742 versus 46,577 certifications in 70,221
recorded island-steps at the saved zero-FP operating points. Their ratio is
1.11089164, a certification-rate ratio. The comparator is the included CA+sleep
implementation. Both P-only 5,754 and comparator-only 589 remain. Weighted skipped
work is potential work calculated after full evolution. No runtime gain was
executed. The fresh three-disk hostile example shows that the source certificate
is not sound on every World input: penetration stabilization can introduce an
impulse absent from its bound. The comparator also fails that example. This does
not alter any historical row or contradict zero observed FP in the saved suite.

The invalidation study preserves removal of 490 settled timer reverifications,
zero active timer refloods, six named volume-drift diagnostics and zero reported
unwitnessed drifts. Rounded average and peak flood costs are unchanged. A touched
cell is a useful locator; it is not proved to be the least sufficient cause of a
joint volume change. Existing grid.set edit invalidation and touch/advection paths
remain necessary. Arbitrary silent array mutation has no guarantee here.

## Scoped paragraph suitable for editorial consideration

In the pinned discrete fluid model, two states with equal water volume and
container geometry but different initial placement produce different right-region
breach answers over 300 frames, refuting that summary for the specified dynamic
receiver. The saved stress panel reports 84 disagreeing pairs among 144; a fresh
replay confirms the original two-state witness. In a separate local rigid-disk
study, a proposed pair-persistence guard certifies 73.6845% of 70,221 island-step
decisions, versus 66.3292% for the included conservative-advancement-plus-sleeping
comparator, with zero false certifications reported at those selected operating
points. Neither guard is universally sound over the simulator's full admitted
state space: a stationary overlapping-disk control separates a certified contact
pair after stabilization. Finally, witnessed invalidation removes 490 timer-driven
settled checks in four reported fluid scenarios while retaining the dominant
flood-work null. These are receiver- and implementation-specific results; they
establish neither a runtime speedup nor a physical fluid law.

## Execution and remaining limits

`python -I -B fluid/check_f1.py --output OUT.json` checks saved aggregate arithmetic
and runs only the original tall/flat pair through unchanged source JS. The optional
word “historical” is never used for those new series. The other new entrypoint,
`fluid/review/check_broadphase_hostile.py`, runs the single consequential defect.
Final-export receipts identify the final code bytes and both executions.

No raw dynamic/invalidation panel corpus or raw70,221 broadphase decision rows
were committed at either pin. Saved Markdown/JSON aggregates and executable source
are included, so their arithmetic and scope can be checked; original row-level
selection, timestamps, runtime costs and universal soundness cannot be recovered
from them. No source-access dependency remains for the bounded F1 assignment.
