# Return to William — C2 completed

F1, AD1 and YM1 are finished at their requested bounded scopes. This return
contains actual source snapshots, working code, results and a portable replay
entrypoint. No dispatch or message relay is needed.

**F1 — fluid claims reconciled.** Both pinned PR12/PR13 sources were retrieved
through read-only GitHub access and checked against their Git blobs. The original
tall/flat dynamic witness replays: equal volume/container, different breach bits,
with peaks 22/0 and first breach 11. Initial placement differs; initial velocities
do not. The saved broadphase advantage is a certification-rate ratio of 1.1108916,
not runtime, and both 5,754 P-only and 589 comparator-only cases remain. The 490
removed timer checks and unchanged dominant flood work are preserved. One small
new overlapping-disk example shows both broadphase guards can falsely certify
outside the saved suite. It narrows universal soundness without changing the
saved zero-error counts. Full historical per-frame rows were not committed;
their absence limits independent replay of aggregate claims.

**AD1 — one working scoped-reuse specimen.** All 30 named cases and 126 bounded
aggregate bags pass for the scoped and conventional implementations against the
direct reference. Same-snapshot answers are reused; reorder/relabel transport
validates new bytes; changed values or scope receive the appropriate new answer
or refusal. Needed missing backing returns OPEN, while unrelated cold-source
loss leaves a supported historical hot answer intact. The result is a functional
tie with ordinary typed caching. The explicit record uses more serialized storage
in this implementation; no accuracy, speed or compression gain is claimed.

**YM1 — one physical bridge completed.** A source-free Maxwell field mode supplies
equal present electric/magnetic energies but different later magnetic energies.
The signed energy-transfer relationship repairs the declared energy receiver
under exact time evolution. The written derivation, complete restricted fibers,
small exact/field calibration and finite-box control are included. The box
excitation energy decreases as 1/L, so positive finite examples do not establish
a common positive gap. This is an Abelian classical bridge plus an inherited
free-mode quantum control; it is not an interacting Yang–Mills gap result.

Already closed and unchanged: C1 exact finite core recovery/implementation;
SAT02 accepted partial research and integrated M1 maintenance, **6 RUN / 4 NOT_RUN**;
Lind–Reichardt review005; the frozen **AD 14/16 versus 14/16** pilot. Their closeouts,
the C1/SAT active archives and paired earlier evidence are carried forward.

The genuine open boundaries are broader RPRM acquisition/approximate grouping,
fiving/Double-Stamp and curve questions; historical source-row verification where
raw rows are absent; broader AD usefulness; and interacting/quantum/continuum
physics beyond the chosen model. None is a missing step in this assignment.

See `WORK_STATUS.json` for check and worker dispositions, `REBRIEF_ADDENDUM.md`
for the continuing state, and `PAPER_ROUTING_NOTE.md` for scoped editorial
suggestions. No manuscript, remote PR or frozen input was modified; no packages
installed, paid compute used, commits made, or publication performed.

The delivered ZIP and its SHA-256 are accompanied by a **separate final-evidence
archive**, produced after fresh extraction. `FINAL_REPLAY.json` there records
execution of the new checks from the actual exported bytes. That evidence is
deliberately outside the already-hashed deliverable. Historical evidence is
carried forward, not counted as fresh execution. The bounded work stops here.
