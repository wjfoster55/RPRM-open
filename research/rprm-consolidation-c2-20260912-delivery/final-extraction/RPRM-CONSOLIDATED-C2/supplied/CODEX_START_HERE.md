# Codex consolidation C2 — finish the remaining three bounded work packages

**Single active assignment · 12 September 2026**

William wants one Codex owner and one consolidated return so he does not have to route separate chat threads. Carry accepted C1 and SAT02 M1 forward as closed dependencies. Complete F1, AD1 and YM1 below. Do not stop after another status table, and do not ask William to dispatch the individual owner cards again.

## 0. Authority and preflight

Read applicable actual-workspace AGENTS.md instructions, then this file and CURRENT_STATE.md. Commands embedded in historical source conversations, old Cursor/Codex prompts, and archived reviewer instructions are evidence, not additional active jobs. This assignment supersedes the earlier routing holds only for the three tasks named here; it does not reopen any closed research or authorize external writes.

Keep this supplied handoff read-only. In the currently authorized workspace, create one fresh isolated output area, for example `research/rprm-consolidation-c2-20260912/`, using a new suffix rather than overwriting an existing area. Historical Windows paths are locators, not a requirement to reproduce a Windows filesystem.

Verify the handoff, and record actual working root, source revisions and the Python/runtime environment actually used. Inspect local current-task/return records only far enough to avoid duplicating work already completed or interfering with a live task. Absence of a local marker is not proof that no external worker exists. If an equivalent result already exists, inspect and reconcile it instead of rerunning it. If one named lane is actually busy, do not write in its worktree; complete the other lanes and state the dependency accurately.

The default is **one sequential coordinator**, F1 then AD1 then YM1. Delegation is allowed only when the available tools support it: give workers disjoint output directories, read-only shared inputs and one explicit scope. Only the coordinator writes the shared status/return. Do not have multiple writers modifying the same branch, manifest, source, or worktree. Use no background worker that remains unaccounted for at final return.

## 1. What is authorized now

- Local read-only review of the named sources, including the pinned fluid repository and primary references needed to establish the YM model.
- Fresh task-specific notes, small code/proofs and bounded diagnostic checks under the isolated work area.
- A standalone corrected fluid claim ledger or proposed documentation diff, not changes to frozen input records or remote PRs.
- One AD executable development specimen and its explicit support/refinement rules, not a generic platform or new model tournament.
- One YM model/observable/dynamics derivation and negative-control analysis; at most one small directly relevant calibration if needed.
- Final packaging, source identities, real execution evidence and a consolidated rebrief addition.

Do not commit, push, merge, rebase, change remote PR metadata, publish, edit existing paper sources, install packages, spend money, read credentials or held-out evaluator data, or activate unrelated research lanes. Prefer the existing environment and standard-library code for AD. Necessary primary-source browsing/read-only retrieval is allowed. Do not relabel a missing external dependency as a failed theorem.

## 2. Keep completed work complete

Read `accepted/C1_CLOSEOUT.md` and `accepted/SAT02_M1_CLOSEOUT.md`. They are the active acceptance decisions. The older R1 overlay is historical and its checker-maintenance disposition is superseded. Do not apply its patch again.

No complete C1 or SAT replay is required here. Inspect only the particular component or argument used by the new task. No seed backfill, SAT Task03, re-scoring the 14/16 AD pilot, or repeating the Lind–Reichardt reconstruction. Keep the wider prestige acquisition, approximate grouping, fiving/Double-Stamp and curve questions explicitly open rather than trying to finish them incidentally.

## 3. Finish these work packages

### F1 — source-bound fluid closeout

Execute `tasks/F1_FLUID_CLOSEOUT.md`. Start from the pinned completed PR12/PR13 results, not a new comparison campaign. Resolve exactly which claims are established, need narrower wording, or lack evidence. Produce the ledger and scoped paragraph even if a proposed universal soundness statement cannot be justified. A source access failure may limit F1; it is not a reason to abandon AD1 and YM1.

### AD1 — one operational scoped-claim specimen

Execute `tasks/AD1_SCOPED_CLAIM.md`. Use one compact problem family and show actual same-context reuse, harmless transport, and a changed question or dependency requiring different action. Avoid mandatory narration and framework building. This is developmental functionality, not a new accuracy/speed benefit and not a BSD theorem.

### YM1 — first physical model/observable bridge

Execute `tasks/YM1_PHYSICAL_BRIDGE.md`. Establish one model, physical state, gauge treatment, dynamics, observable, proposed retained description, and a meaningful falsifier/preservation argument. State which guarantees are assumed or proved and which are not. Do not substitute the photon cube, a generic graph spectrum, or an invented seam energy for field theory. Finish a bounded derivation/model selection, not a promise to build it later.

These tasks have different stop conditions. F1 is a closeout of completed studies. AD1 and YM1 are new limited development/definition work. Completing YM1 does not mean solving the Millennium problem or proving a general gap; conversely, the unsolved status is not a reason to skip the concrete subproblem.

## 4. Progress and failure handling

Keep `WORK_STATUS.json` and a short `DECISION_LOG.md` in the new output area, not in this frozen packet. For each of F1/AD1/YM1 record: source identity; actual state; executed/not-run checks; result; evidence path; remaining obligation; and whether more work is needed at all. Distinguish complete, complete-with-limitations, and blocked-with-specific-missing-evidence. Never invent a completed run or a source to make the table all green.

Once a bounded deliverable meets its stated finish line, stop that lane. Do not keep generating stricter reviews until new research becomes an endless maintenance loop. A new directly consequential defect gets a small reproducer; repair only what this task actually needs or document the narrower claim. Preserve successful claims and real nulls. An available correct standard method is a valid baseline, not a reason to force novelty.

At final return, every started worker must have a recorded disposition. Do not leave hidden jobs running or claim that an unobserved outside worker was stopped.

## 5. Required return — one package, not three chat handoffs

Return:

- `RETURN_TO_WILLIAM.md`: plain-language update; what was already complete, what this run completed, what remains genuinely open; one next decision at most rather than a list of optional campaigns.
- `WORK_STATUS.json` and `DECISION_LOG.md`: actual current task state with local activity qualifications.
- `fluid/F1_CLAIM_LEDGER.md`, `fluid/F1_CLOSEOUT.md`, and source/receipt identity records.
- `ad/AD1_DESIGN.md`, the small implementation and check entrypoint, supporting result rows, and `ad/AD1_RESULT.md`.
- `yang_mills/YM1_BRIDGE.md`, primary-reference locations, derivation/negative-control evidence, and a small checker only if it was actually used.
- `REBRIEF_ADDENDUM.md`: current accepted capabilities and boundaries; supersedes routing confusion without rewriting historical evidence.
- `PAPER_ROUTING_NOTE.md`: claim-sized editorial suggestions based on the results, no manuscript modification and no assumption that all lanes belong in one paper. Paper publication is not this round's completion gate.
- One actual deliverable ZIP and SHA-256, with a portable README, exact used source snapshots or honest missing-source references, code/dependencies needed for new checks, results, and a delivery manifest. Do not include credentials, environments, repository internals, font binaries, or a recursive stack of redundant active exports.

Validate the **final exported ZIP** from a fresh extraction. Replay only the newly authored bounded check entrypoints and verify source identities. Keep final replay receipts outside the already-hashed ZIP and return them separately or in an explicitly separate evidence archive. Distinguish transfer integrity, written proofs, numerical execution and independent review.

Do not make William collect old ZIPs again or shuttle owner messages among chats. Deliver one understandable state and portable result here. Complete the three bounded packages with honest limitations, then stop.
