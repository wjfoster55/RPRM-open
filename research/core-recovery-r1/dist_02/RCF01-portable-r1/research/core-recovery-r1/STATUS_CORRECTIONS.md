# Status-meaning corrections (RCF01-R1)

These are corrections to the first Cursor return
(`research/core-recovery-01/RETURN_TO_WILLIAM.md`). They are not backdated
source or protocol changes. The frozen 22-case protocol
`RCF01-envelope-1` is unchanged.

## What a fiber/status counts

| Report wording | Named object | Correction |
|---|---|---|
| Boolean C2 summary → MANY(2) | **Boolean-valued source laws** with that C2 | Remains correct for that family. Independent Q-sums of `{0,hxy}` are a different question: 3 full outputs, 1 C2. |
| with omitted site → ONE | **Unique full table** once site 7 is supplied | Unique exact summary can still coexist with other full laws if the omitted site is not actually supplied. |
| missing cold + admitted flip → OPEN / REOPEN_REQUIRED | **Admitted operation** whose hot summary is insufficient | Keep `REOPEN_REQUIRED` for this branch. `OPEN` names an unresolved remaining-information question, not an empty law family. |
| stale bytes → NONE / INVALID_DEPENDENCY | **Verified backing artifacts** for this promised construction | Not a proof that the mathematical candidate family consistent with an intact C2 is empty. |
| PE02 ONE/EXACT compact arithmetic | **Query/output summaries** (unique C2 of `+`/`*`) | Not unique underlying full laws. |
| B6 dwell NONE / UNSUPPORTED | **Admitted dwell successors** | B6 exists as a state. Vertex-count 3 is shared with A5. Zero admitted dwell successors is not an empty state family. |
| PE19 UNSUPPORTED vs REOPEN_REQUIRED | **Contract admission** vs **admitted-but-insufficient hot data** | The retained development FAIL expected the wrong refusal. Do not overwrite that receipt. |

## Finite fiving

Say "this round's finite model of fiving" (`d=5h+r` on C10, adapter `A_r`).
Do not silently treat that C10 example as the whole source fiving concept.
The splice `{0,5}` commuting with Double-Stamp restart remains OPEN.

## API witness field

Successor `add_units` / `multiply_units` witness `counts` is
`unique_C2_output_summary`. Candidate-family propagation for Q-sums is
outside this API.
