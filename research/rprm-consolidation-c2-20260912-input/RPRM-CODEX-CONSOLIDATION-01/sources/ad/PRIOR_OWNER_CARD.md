# BSD / Absolute Distinction owner — separate two different next questions

## Preserve what is complete

Read `sources/library/REVIEW(3).md`: review005 closes the classical Lind–Reichardt reference example. Local constructions, the covering certificate and global-obstruction argument should travel together. This is not a new rank computation, total Sha enumeration, L-function result or BSD proof. Do not restart that example because the recovered core is now complete.

Read `sources/library/REVIEW(4).md`: the pilot_collection_v2 primary score remains conventional14/16 versus protocol14/16. Both arms have correct arithmetic at the two scope-disputed labels. Preserve the frozen scoring and add scope/provenance annotations without retrospective rescoring. The protocol produced more raw text; no measured benefit or latency/cost inference follows.

Read `AD_RECOVERED_CORE_INTEGRATION_NOTE.md` as a historical integration proposal. Its “core currently running” status is superseded by C1 acceptance, but its semantic distinctions remain relevant.

## Proposed next AD development task

Build or specify exactly one compact claim record with an executable use boundary, preferably using an existing SQL/arithmetic adapter rather than a new generalized framework. Distinguish:
- a statement and quantified domain;
- the current instance and actual source identity;
- the questions/operations justified by the retained evidence;
- the evidence dependencies and a real recovery route;
- a unique answer from a unique full source object.

Use one small matched sequence with three transitions:
1. A repeated same-context query where reuse is genuinely valid.
2. A coherent reordering or representation transport that preserves the answer and must not cause unnecessary rejection.
3. A changed source, operation or quantifier that exposes a missing distinction, requiring justified refinement, recomputation or an unresolved outcome.

The existing SUM/DISTINCT or scoped-join examples are useful development fixtures. Distinguish current-data equality from a schema-wide substitution theorem. A schema-level theorem whose assumptions survive need not be invalidated just because a row changed; an instance-only answer must not acquire a schema guarantee. Do not answer every invalidation question with “recompute everything.”

Compare with a competent ordinary implementation using the same evidence and limits. Score correctness of reuse/transport/refinement decisions, actual supporting information, and total work. Do not reward mandatory RPRM narration. Do not count old exposed examples as new validation, and do not rerun the 32 old prompts to get a better score. Any prospective evaluation gets a later fixed version, new tasks and verified dispatch conditions.

## BSD mathematics remains a separate lane

If the next aim is a BSD result rather than an AD method result, identify a concrete new arithmetic quantity or construction and state the theorem connecting it to the desired global alternatives. More repetitions of local-solubility bits cannot distinguish members already admitted as locally soluble. The full analytic/global relationship is not supplied merely by source retention or the cube.

Do not let a software or prompting study silently become evidence of an arithmetic theorem. Conversely, a method-study tie does not refute the exact arithmetic or every possible use of the method.

Return the single development question, exact adapter/contracts, existing-source reuse, and the new operational result or explicit bridge still missing. Do not expand into a second 32-session campaign, repair unrelated core code, edit a published manuscript or restart closed arithmetic under this card.
