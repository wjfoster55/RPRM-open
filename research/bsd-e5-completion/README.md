# BSD E5 completion

Read [RETURN_TO_WILLIAM.md](RETURN_TO_WILLIAM.md) for the explanation and
[BSD_REBRIEF.md](BSD_REBRIEF.md) for the short mathematical handoff.

This continuation closes the primitive-generator, odd-primary/total Sha,
and full leading-coefficient-formula questions left open by E5 test01.
It also checks explicit coordinate changes and preserves the user's partial
carry/swap trace. Each result retains its stated carrier and evidence grade.

Python 3.10 or newer, standard library only. From this directory:

```text
python -B work/verify_delivery.py
python -B work/run_all.py --output runs/my_fresh_run
```

Use a new output path. The runner verifies the preserved review, executes
the new generator, factor and coordinate calculations, then independently
reassembles the finite evidence and the integer-isolation calculation.
Its output does not claim to re-prove the cited global theorems.

Detailed proofs: `GENERATOR_PROOF.md`, `BSD_FACTORS.md`,
`BSD_SHA_THEOREMS.md`, and `COORDINATE_NOTE.md`. Results are indexed in
`CLAIM_LEDGER.json`; fresh execution evidence is under `evidence/` and
`runs/final_validation/`. `supplied_review/` preserves every supplied member,
including the embedded prior sources, without modification.

The active user request explicitly reopened the prior arithmetic frontier.
Historical instructions to stop at test01 and the review's lack of a new
dispatch are preserved as source context; they do not override this new
request. All work stays in this separate continuation directory and its ZIP.
No other research lane or published document is edited.
