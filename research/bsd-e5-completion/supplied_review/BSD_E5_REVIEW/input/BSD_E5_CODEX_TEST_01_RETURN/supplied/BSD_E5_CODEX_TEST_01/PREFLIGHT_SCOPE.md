# Packaging verification — 12 September 2026

The two input programs were replayed with their original source bytes, under
Python 3.13.5 in the assistant's working environment. Their selected mathematical
records match the supplied prior receipts. All 17 preserved source/context files
match their source ZIP members before and after execution.

The replay includes the source's 256 exact decoder examples, finite coefficient
and positivity calculation, and 32-vertex cube recoding. It is not a newly written
independent verifier, an independent proof review, a general BSD result, or a
Codex execution. The original analytic program ran its optional mpmath illustration;
that floating computation is not part of the exact rational sign proof.

The launcher and bundled source scripts parse as Python, and the supplied JSON
files parse. Package reconstruction was checked by extracting the finished ZIP
and rerunning the launcher from a different working directory; that result is a
portability smoke check of the same code, not independent mathematical evidence.

Codex's independent proof/code checks and the stronger two-sided derivative
interval remain NOT_RUN at delivery. The assignment asks for those results now.

`preflight/REPLAY.json` records the actual source hashes, commands, exit codes,
timings, reproduced mathematical fields, and the distinction above. `TEST_PLAN.json`
contains the fixed tasks and six controls for the Codex run. Those controls have
not been executed merely because they appear in the plan.
