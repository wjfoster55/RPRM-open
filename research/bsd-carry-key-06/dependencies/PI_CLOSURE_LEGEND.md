# The Pi-Closure Legend

The digit roles, inside the declared closure (base ten, the eleven-digit viewfinder,
the seven-record family, the involution package). Within this closure these roles are
stable and citable without preamble. Outside it, a role travels only with an adapter.

Tags: **[proved]** = theorem, machine-checked (PiDigitSymmetry or a sealed packet).
**[reading]** = William's operational reading — licensed inside the closure, typed
candidate outside it. A line can carry both.

---

## The digits

**0** — additive identity; with 5 it is the held-still pair of N (the 2-torsion {0, 5})
[proved]. P sends it to 5 [proved]. Plays four distinctly-typed roles across carriers
(numeric zero, NoActive, empty, port) which the corpus keeps separate [sealed].
*Reading:* the confusing one; approach through its type, not its glyph.

**1** — multiplicative identity; start of the count; root of the ancestry 1 → 3 → 7
under double-plus-one [proved: the address-tree left boundary]. P sends 1 to 9 [proved].
*Reading:* "ones tell us something, but not a lot" — consistent with 1 sitting in the
big ordinary orbit, not a distinguished pair.

**2** — the gap (B − A at pi's record) [proved]; the even prime; the doubling operator's
own number. P sends 2 to 8 [proved]. *Reading:* the span receipt — the 2 in 325 is
abducted from the 3 and the 5 (3 + 2 = 5 checks itself) [sealed as ONE_EQUIVALENT].

**3** — pi's lead; held still by P; a square root of −1 (3² = 9 ≡ −1); a generator of
the units [all proved — and base ten is the only base where those three descriptions
name one pair]. *Reading:* "when I see a three I know there's a nine" — typed echo:
3² is 9, and 9 is −1 wearing digits.

**4** — the abducted middle: 3 + 5 = 2·4, present between the endpoints without being
written [proved as the midpoint identity; "abducted" is the closure's word for it].
2²; the Hamming lock m = 4 (the one-time parameter coincidence) [proved]. The number
of Klein-group elements [proved]. *Reading:* "345 tells you you're in the box."

**5** — the B-endpoint; held still by N (it is half of ten — the 2-torsion partner of 0)
[proved]; P sends 5 to 0 [proved]. Ten's odd prime half (10 = 2·5). With 3 it is the
unique endpoint pair whose grammar closes to all ten digits [sealed, bounded uniqueness].

**6** — P's partner for 4 [proved]; the shared decoder corner at the target: gap +
midpoint = 2 + 4 = 6 = 110₂, the address one light short of the top [proved identity].
2·3. *Reading:* "six is fake" — inside the closure this types as: the target record's
*role-value* 6 is always a computed corner (a sum wearing a glyph). The glyph 6 does
occur in the source window (…92**6**…) — role and glyph are different occurrences
[scope repaired per C002 audit].

**7** — the other square root of −1; held still by P [proved]; the record count; the
top address 111 — all three parity lights on [proved]; the end of the ancestry
1 → 3 → 7 [proved]. *Reading:* the all-on state; where the count completes.

**8** — 2³; P's partner for 2 [proved]; concurrently 2×4, 4×2, and 4+4 — the
affordances are simultaneous, not competing [exact]. The impossible rung: within the
declared shell-ladder family (bounds αA+βδ ≤ c), no base ever grows the census to a
fourth rung [proved for that family; scope repaired per C002 audit — this does not ban
all conceivable linear carriers]. *Reading:* every 8 is a 4 and a 4
with one half abducted — the road to 15 is not growth but seam: 7 + 7 + 1, the tree
plus its shadow plus the join [reading; typed candidate, untested — the fake-half rule
is William's to spec].

**9** — minus one in digit clothing (9 ≡ −1 mod 10) [proved]; 3² [proved]; N's partner
for 1. *Reading:* "nine-one is a carry about to happen" — exact positional arithmetic:
…9 + 1 rolls the column. The end of a cycle, one step before the seam.

---

## The load-bearing combinations

- **3 | 5** — the endpoints; the unique grammar-closing pair; the record at the top
  address [proved]. **345** — endpoints plus their abducted middle [proved identity].
  **325** — endpoints plus their span, self-checking [sealed].
- **1 → 3 → 7** — double-plus-one ancestry = the address tree's left boundary [proved].
- **4, 2, 1** — the shell census; the three parity positions; 4+2+1 = 7 [proved].
- **35 → 24 → 13 → 02** — the subtract-11 ladder = the δ=1 shell descending, wearing
  addresses 7, 6, 5, 4; the three ladders together sweep all seven records, addresses
  7 down to 1 [proved 2026-08-21; base 13 carries a five-rung version — the four-rung
  all-ones-top ladder stays unique to base ten].
- **{0,5} / {1,9} / {2,8} / {3,7} / {4,6}** — the five pairs; the partition; every digit
  held still by exactly one of P, N, T [proved]. The word group these pairs generate is
  an honest 1024 on the window [proved].
- **141** — the window's opening row; occurs in the stream at exactly ordinary density
  (rank 401/1000) [proved] — its weight is positional (it is *the first* one), not
  statistical. **969** — 141 seen through P [proved identity].
- **9 next to 1** — carry brink [arithmetic]. **3 next to 9** — a square beside its
  root [arithmetic].

---

## The three shadows (what "anti-pi" actually names)

1. **P-pi** — the stream through P: 39690184030… [proved identity, ONE_DEPENDENT].
2. **N-pi** — the stream through N: 79695184575… [proved identity, ONE_DEPENDENT].
3. **4 − pi = 0.8584073464…** — the dual-cell partner: the nines-complement stream,
   the honest "other side" that needs no construction at all [arithmetic].

All three are the window's own reflections — computed from it, never observed beside
it. The closure's rule: a shadow confirms the mirror, not the body.

**The T-fixed opposite-half pair** [sealed, C002]: William's blurted set
support(1234679) = {1,2,3,4,6,7,9} and the 4−pi prefix support {0,3,4,5,6,7,8} are
both fixed as sets by T, and each differs from its own P-image only inside the orbit
{2,8} — occupying *opposite* members of it. Exactly 32 of the 1024 possible digit
supports have this property, and the two sets' symmetric difference is precisely three
complete P-orbits. Post-hoc, no confirmation claimed; the next tooth is a preregistered
application to genuinely held-out sources. Independent anti-Pi remains open — lane four
of four.

---

## The closure's boundary (one paragraph, so the legend stays safe)

Inside the closure, these roles are exact and citable bare. The closure's edge is the
same in every direction: the roles were selected at the window and do not travel into
the stream (the decoder fits only at home; no local law to order 6; densities ordinary
in bases 5, 10, 14, 18) — so any role asserted PAST the window is a typed candidate for
the wizard lane, not a citation. That boundary is what makes the inside safe to speak
plainly. One legend, two lanes, no preamble.
