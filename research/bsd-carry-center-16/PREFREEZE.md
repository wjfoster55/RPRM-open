# Bounded atom / centered zero / carry investigation

The user suggests that atom diagrams may expose a relation between rings
and central blobs, and that the nine seam carries to 0/1, with five as a
centered zero. Treat these as hypotheses to make explicit, not as universal
digit axioms. Preserve the distinction between source evidence and task
authority. No earlier packet, other research lane, paper or registry changes.

C1. Reuse the documented Seam Zero map z(x)=5-x on the source rail
{4,5,6}, with tau(x)=(6-x)/2. Explicitly extend only z to the bounded
decimal carrier {0,...,9} for testing the proposed comparison of 5 and 9.
It is injective and reverses differences. Applying different centers can
make both displayed values zero, but the centers must be retained.

C2. Interpret 0|1 as a candidate two-field record (digit=0,carry_row=1),
not the rational 0/1. Use digit d and row q in {0,...,9}, readout 10q+d.
Increment is partial at value 99. Test all 99 admitted steps, complete
digit-only fibers, inverse steps, and enabledness lost at the top row.
Reaching the end requests an explicitly larger carrier, not a silently
repeated infinite cycle. Compare the ordinary arithmetic additions of
0/1 and 1 to both numbers: neither changes their difference.

C3. Test the conditional stopping law: if D/delta is an integer,
delta>0, and |D|<delta, then D=0. The attributed BSD interval has width
and absolute endpoints below 0.001, but provides no premise that
D/0.001 is an integer. Retain D=0.0001 as a rational hostile case.
Refining the decimal representation preserves this nonzero D.

C4. Research atom diagrams using primary official sources. Separate
nucleus and probability-cloud displays from literal planetlike orbits.
Study only a toy circle wave psi_n=exp(i n theta), n in {-2,-1,0,1,2},
with normalized angular measure. Density can forget winding. A written
proof may cover the finite Fourier span and its constant-mode exception.
No claim that the toy circle is a physical atom or a BSD model.

Outputs: short understandable explanation, runnable exact checks,
source-backed atom note, retained exceptions, and concrete missing BSD
relation. The user's intended full 0/1 grammar remains OPEN pending
selection of an operation and receiver.

C5 (added before its tests). Use the dimensionless ratio Q=A/B1 from the
attributed intervals and the candidate family f(theta)=exp(i*(Q-1)*theta).
The bounds put Q-1 in (-1/2,1/2). If f is proved single-valued on the
ordinary circle, periodicity forces Q-1 to be the integer zero. Defining f
on a real interval does not prove that endpoint compatibility; it is the
missing integrality premise in another representation. Do not assert it
for actual BSD. Test interval-compatible Q!=1 and twisted boundary cases.

User clarification: the meaning of 0/1 depends on context; test the carry
reading without promoting it to the uniquely intended interpretation.

C6 (after actual scout output, before this test). The existing P digit
involution sends 5->0,9->1,01->59. Test its coordinatewise action on
all width-two words and the experiment-15 maximal reverse-overlap F.
Candidate equation P(F(w))=F(P(w)) follows from preserving equality and
order of digit occurrences. Test 59->01,595->010 and successor conjugation.
An opcode field is a separate type; never map it as a payload digit by
accident. This candidate is prompted by the current observation and the
existing map, not a pre-observation prediction of a BSD bound.
