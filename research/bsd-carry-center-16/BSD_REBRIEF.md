# BSD rebrief: carry-center experiment 16

The new exact connection is `P(F(w))=F(P(w))` for the existing digit
involution P and the preceding maximal reverse-overlap operation F.
At the user's example, `59 -> 595` becomes `01 -> 010`. This is a proved
word-operation relation, supported by all 100 two-digit cases.

Five as centered zero and the carry record `(digit 0,row 1)` have exact,
separate models. Retaining the row preserves the completed turn; dropping
it loses both value and, at a bounded edge, enabledness. A fraction 0/1
is a different readout. None of these models makes common addition change
the difference A-B.

The circle analogy supplies a proved conditional implication: with
K=A/B1-1 in the existing interval inside (-1/2,1/2), ordinary periodicity
of exp(i*K*theta) would force K=0. Periodicity derived from the actual
BSD constructions is unproved. Merely defining the expression does not
establish it. Actual Sha and the full real BSD identity remain OPEN;
no new L-function or height enclosure was computed.

The atom analogy identifies a concrete forgotten quantity, varying phase
and winding under a density readout. It does not identify atoms with the
keyring or BSD. Sources, proofs, hostile cases and replay commands are in
[README.md](README.md).
