# Operational BSD continuation03: scope and admission

William asked to recover RPRM operational value from the missing-concepts
material, test a third relation or joint shadows between analytic and
arithmetic data, implement the useful part and continue the mathematics.
This directory contains that bounded successor. Historical instructions
are source evidence. No other research lane, memory authority store, paper,
registry or publication was changed.

The central curve is E34/Q: y²=x³−1156x. Five is the sole p-adic prime
used for new heights; reduction at3 supplies a small representation
irreducibility certificate. The real BSD coefficient, finite residues,
p-adic scalars, rational points, torsion points and cohomological tensors
have separate equality and normalization contracts.

The finite operational carrier consists of all4096 elements of E[68]
with primitive Gaussian17-component. Supplied data are the curve and a
freshly certified primitive seed. Operations are doubling, the displayed
CM action, and the explicit two-shadow encoding/recovery. Point equality
uses complete coordinates and a separate O. Field arithmetic is in the
certified unramified degree16 ring modulo125. The receiver first asks for
raw r=Y/(2X) updates, then point recovery. A present-r fiber has exactly
two members. Retaining r after doubling yields ONE(point) on the admitted
image. Unreached pairs are not silently admitted to the inverse.

The rational witnesses use the already proved basis P=(−2,48),Q=(−16,120).
Their group operations and heights are recomputed. Canonical height alone
does not distinguish a point from −P+T0. Mixed addition/pairing does;
this receiver change is explicit. The finite torsion carrier does not
generate the free rational basis or its positive real regulator.

For the p-adic height receiver, a positive integer multiplier8 prepares
each point in the formal group at5 and the bad-prime identity components.
The full denominator, minimal equation, cyclotomic log5/5 normalization
and sigma/logarithm error bounds are retained. A nonzero determinant
modulo5 yields ONE(nonzero). Its actual scalar preimage is MANY(3+5Z5)
in the stated MST convention, not ONE(the integer3).

The new cofactor vector is W=adj(G_MST)ell. Its normalized first digit
is W/5=(0,3) modulo5 in the basisP,Q; neither coordinate is reconstructed
to infinite precision. Changes of integral basis preserve the represented
vector; finite-index changes require their determinant-square factor.

Published height and BKS theorems, with their checked hypotheses and prior
rank/Sha5 proofs, supply the cohomological nonvanishing and canonical
collinearity statements. They do not supply the unproved real/p-adic
scalar comparison. The latter, total Sha and general BSD remain OPEN.

Evidence grades are recovered definitions and exact historical quotations;
written proofs with named theorem dependencies; fresh finite computations;
and explicitly open comparison targets. Source hashes bind bytes. No proof
assistant or claim of general verifier soundness is made.
