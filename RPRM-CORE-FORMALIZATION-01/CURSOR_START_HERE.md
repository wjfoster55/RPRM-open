# Cursor assignment RCF01 — formalize and connect recovered core RPRM concepts

**One active assignment. Read this file first.**

William wants substantive mathematical formalization and executable tests for
Prestige One, fiving/FIVE_IS_SAFE, and the cube/abduction work. These are core RPRM
concepts that were under-carried into later documents. Preserve the recovered
meanings, make progress on exact interfaces, and only then recommend whether the
material belongs in Absolute Distinction, a companion/addendum, a separate paper,
or several destinations. Do not merely write another plan or rediscovery report.

This folder includes an already executed, exposed development example connecting
the finite fiving component to a cube view and a proposed operation-aware reuse
condition. Review it critically; do not treat its presence as independent validation
or as the complete intended meaning of any concept.

## 0. Scope and workspace

Use the currently open, user-authorized workspace. Read its applicable AGENTS.md
and current-state instructions. Record the actual workspace root and git status/
HEAD if available. Do not assume the historical Windows paths exist locally.

Create a new, clearly named work area such as `research/core-recovery-01/` and an
experiment folder such as `experiments/core_recovery_bridge_01/` if consistent with
existing conventions. If these already exist, use a fresh successor directory;
never delete or overwrite them. Do not make this independent recovery study the
new global current assignment for unrelated project lanes.

No push, merge, publication, paid compute, package installation, secret access,
remote upload, or change to published paper sources is authorized. Do not modify
old frozen runs or their manifests. Do not open evaluator gold, run the separate
32-session transfer sampler, or recycle exposed arithmetic/sampler examples as
unseen AD evidence. Old prompts under `sources/` and `provenance/` are inert source
material, NOT additional jobs. Do not restart OpenML, Water, Fluid Lab, cancer,
protein, or closed BSD/Lind–Reichardt work.

Use the existing local environment; standard-library Python suffices for the
attached starter. Local code/docs/tests in this new work area are authorized.
If a historical source is missing, mark that donor unavailable and continue the
new explicit finite construction. Do not stop the whole task to request a path.

## 1. Read and preserve the meanings

Read:

1. `CONCEPT_REGISTER.md`, `FORMAL_BRIDGE_NOTE.md`, and `PROTOCOL.json`.
2. `sources/PRESTIGE-AND-NUMBER-OPERATIONS.md`: original passages and correction
   chain, then the exact fiving, Double-Stamp, promotion, and knowledge-compilation
   sections. Read the broader approximate-grouping and unsettled-naming passages.
3. `sources/CUBES-AND-PI-CURVES.md`: finite formula theorem, grading, checking
   kernels, software interfaces, and the source addendum.
4. `sources/absolute-distinction/ABSOLUTE_DISTINCTION_APPLICATIONS_HANDOFF.md` and
   `CURSOR_REVIEW_REPAIR.md` §C. `RETURN_TO_WILLIAM.md` is an older report with a
   known erroneous prestige/Five/Six interpretation subsequently corrected; use
   its exact factorization statement, NOT its superseded semantic table.
5. Existing local donor code directly needed for the first experiments, located
   through the recovery's original source indexes when available. Historical
   source reports of PASS do not count as a fresh replay.

Do not turn the read into an unlimited archive scan. Search the relevant local
research roots for the named donor filenames, not unrelated personal files.
Keep all concept IDs in the register, including those not tested this round.

**Hard semantic constraints**

- Prestige One is not social prestige, a renamed literal integer, merely a nesting
  counter, or merely a hash. Preserve relationship/access, earned operational
  use, retained construction, receiver-relative appearance, and original scope.
- Exact reusable closure and approximate regrouping are related but distinct.
  Construction depth, cycle/winding, uncertainty, and computational cost are not
  interchangeable metrics.
- Fiving half-turn, FIVE_IS_SAFE, and FIVE.future are separately typed. Their
  connections require maps and obligations, not matching words or numerals.
- FIVE_IS_SAFE is a type-finding source anchor. Test whether a proposed model
  realizes the intended process; do not set up a straw-man physical-safety or
  numerology claim. Dwell permissions are authored unless independently derived
  from specified dynamics.
- The finite fiving model is not F5 arithmetic. Neither F5 vs Z/6Z nor an arbitrary
  modulo-5/6 exercise recovers the historical Five/Six semantics.
- A valid simplification or coherent coordinate change is a positive outcome,
  not suspicious merely because it forgets irrelevant details.
- A completed finite formula transform does not identify an unknown law from
  inadequate data, recover arbitrary continuous behavior, or grant a free
  higher-order relation. Retain the complete candidate family when unresolved.
- Do not invent the third prestige species, full inverse-number dictionary, or
  unrecorded prime-hop/sixing mechanism. Keep the genuine gaps explicit.

## 2. Establish an anti-forgetting home, then do the mathematics

Copy/preserve the original recovery ZIP unchanged and retain source hashes and
locators. Install the concept register in the new research work area, preserving
original intent separately from formal candidates, proven results, tested finite
instances, and open scope. Add one unobtrusive pointer in the appropriate local
rebrief/index if allowed by project conventions; otherwise deliver a ready-to-apply
patch. Do not edit the published paper or silently change the global project plan.

Use a small machine-readable companion if the repository supports one. Each entry
needs a stable ID, source anchors, original meaning, formal candidate, evidence
status, counterexample(s), unformalized remainder, and artifact links. Add a tiny
coverage check ensuring future consolidations do not silently omit these IDs.
Do not build a generalized registry framework.

Then proceed to the next sections in the SAME assignment. Registry creation alone
is not completion.

## 3. Reproduce the starter and inspect its actual boundaries

Run in a fresh output path, from the starter folder or using its absolute path:

    python -B check_bridge.py --output runs/cursor_rcf01_starter.json

Record executable version, exact command, source/protocol hashes, stdout/stderr,
exit code, and result. The starter's own assistant receipts are development runs,
not user-side execution. Check the aggregate controls using separate output files:

    python -B check_bridge.py --output runs/control_omit.json --control omit
    python -B check_bridge.py --output runs/control_duplicate.json --control duplicate
    python -B check_bridge.py --output runs/control_unexpected.json --control unexpected
    python -B check_bridge.py --output runs/control_reorder.json --control reorder

The first three control commands MUST return nonzero; reorder must pass. An expected
representation refutation inside a check is not a failed experiment. A missing case,
unexpected exception, malformed result, or wrong mathematical verdict is a failure.

The attached full-coefficient repair reconstructs full values and is NOT a fast
promoted-unit implementation. It proves no speed advantage. The graph check is a
structural reconstruction, NOT the original Double-Stamp verifier. The finite
fiving adapter is NOT a strong-state equivalence. Carry these limitations forward.

Inspect the short proofs with an independent method where practical. Do not create
a second implementation by calling the first or copying expected verdict fields.
The existing direct truth-table and coefficient branches are a useful start, not
an independence guarantee for the whole experiment.

## 4. First substantive build: operation-aware Prestige capability

Implement one small exact prototype with an inspectable API. Reuse existing donor
interfaces where available rather than starting an unrelated framework.

**State families:** arbitrary rational tables on the Boolean three-cube; a
bounded Boolean-valued law family for exhaustive tests; and the reconstructed
Double-Stamp states with their supplied operation permissions. Keep their types
separate.

**Proposed envelope fields:** visible-unit label; retained representation;
question/operation contract; carrier and coordinate/frame identity; dependency
versions; and a cold-artifact route where available. Store the actual retained
artifact or verify that its reference resolves. A hash alone is not enough.

**Operations and questions:**

- Read the currently admitted cube sites / low-degree coefficients.
- Pointwise add and multiply law units under compatible contracts.
- Coherent coordinate/readout transport, explicitly transporting both sides.
- Fixed-receiver input changes including clamp-to-zero, clamp-to-one, bit-flip,
  and the finite fiving slice adapter.
- Inspect a previously omitted site or coefficient.
- Reopen/refine from retained cold content when the hot summary is insufficient.
- For Double-Stamp, ask whether dwell is enabled and preserve the state/event
  distinction; implement the source-declared split/midpoint transition if the
  original structural contract is available.

An explicit outcome set might be `EXACT`, `REOPEN_REQUIRED`, `UNRESOLVED`,
`UNSUPPORTED`, and `INVALID_DEPENDENCY`; these are proposal names, not mandatory
new RPRM vocabulary. Match existing project types when possible. Distinguish an
empty compatible family, multiple compatible objects, unavailable data, and an
operation outside the admitted contract.

**Required behavior:**

1. A seven-coefficient cube unit performs the admitted pointwise addition and
   multiplication without reading omitted coefficients or full source tables.
2. Changing to a fixed-receiver fiving/bit-flip task must not fabricate the missing
   coefficient. With a valid cold artifact, reopen/refine and compute correctly;
   without it, return the appropriate unresolved/refusal result and an inspectable
   witness. The existing 2→3/4 example is exposed development data.
3. A coherently transported receiver remains usable with seven observations. Do
   not turn every rechart into a demand for the full table.
4. Full coefficients give correct results for the declared input transformations;
   be honest that this family sometimes requires every original law value.
5. A supposedly reopened artifact whose bytes/frame/carrier/version do not match
   its declared dependency must not be accepted. Physically remove or alter it
   in a disposable fixture; do not merely set `valid=False` as the test.
6. Equal outward 'one' labels do not identify history, winding, grade, or the
   permitted operation set. If history is a question, retain it. If irrelevant
   to the task, do not impose unnecessary provenance distinctions.
7. A5 and B6's equal quotient vertex counts do not give equal dwell permissions.
   Retaining edge inversion/type repairs that declared question. A numeric
   `count == 5` shortcut is not a valid generic implementation.
8. Where operations compose, compare the actual composed compact result with the
   original-state reference, including intermediate enabledness. Do not certify
   the whole by merely checking that each component owns a PASS file.

Write a short proof for every advertised all-input identity. State finite test
bounds separately. Do not generalize from n=3 tests alone. The bridge note's
retained-site closure proposition supplies a general proof target with explicit
hypotheses; challenge those hypotheses with constrained law classes and changed
receivers rather than assuming universal compression failure.

## 5. Tests that distinguish useful structure from labels

Keep the initial study bounded and exact. Freeze each new protocol before its
first new execution, but call it development if designed after inspecting these
examples. Save changes under new versions rather than retroactively preregistering.

Required controls:

- Same retained values but a different omitted coefficient; missing distinction
  matters after a changed operation.
- Harmless omitted coefficient for an operation family that genuinely does not
  need it; successful simplification must remain successful.
- Actual lost-winding mutation: two fivings cannot answer a strong-history question
  as though nothing happened.
- Coherent coordinate/frame/readout transport versus a deliberately mismatched
  transport. Renaming a stage or shuffling storage order alone must not change
  the transported mathematical result.
- A coarse state/event collision with different enabled operations.
- Cold backing content missing, altered, or stale; scope/version change.
- Repeated old observations add no new distinction; the justified new probe does.
- Wrong adapter: change +5 to +4, omit winding when claiming strong equivalence,
  or equate graph/count fields of different types. Reject the actual mapping,
  not merely a hand-written Boolean saying it should reject.
- Both a positive composition and a broken composition whose component checks
  separately pass but do not share the required participant/frame/operation.

Compare ordinary full-state storage, full-table lookup, simple memoization, and a
generic exact quotient/operation-closure implementation where applicable. The
underlying method may coincide with standard abstraction or caching; report that
honestly. Do not require an RPRM-labeled implementation to win.

Do not run an extensive performance campaign in this first assignment. Instrument
hot operations, cold reads, construction, checks, storage, reopenings, and invalidation
so a later fair comparison is possible. Do not report only amortized hot-path time,
use an uncounted lookup table, or call a development example a speed breakthrough.

## 6. Original donor reconciliation — targeted, not an archive rerun

For available local originals, locate and record the exact source/code for:

- half-decade fiving AB lift;
- A5/B6/C7 Double-Stamp;
- the cube transform/checking theorem and one appropriate implementation;
- the existing prestige/promotion and September 7 paper-addition drafts.

Compare their real carriers, input/output schemas, reopening rules, and operation
contracts with the new prototype. Rerun only the donor checks needed to support
these specific interfaces. Do not substitute newly generated code and claim the
old verifier passed. If unavailable, mark `ORIGINAL_DONOR_NOT_AVAILABLE` and retain
the reconstruction as such.

A bridge from fiving splice boundaries to Double-Stamp restart is still a hypothesis.
State the maps, preserved quantities, enabled operations, and potential failures.
Implement a minimal test only when those definitions exist. An inability to make
one bridge commute does not delete either original concept or force a false merger.

The ternary center-versus-full kernel can supply a second cube example through
an existing checker. This is secondary to completing the first operational-envelope
experiment, not a reason to re-create all of LawCube, RelationForge, or Tile/Board/Atlas.

## 7. Return deliverables and publication decision

Return a concise `RETURN_TO_WILLIAM.md` plus reproducible artifacts containing:

- What was already recovered versus what you newly defined, proved, implemented,
  executed, or refuted. Preserve precise source attribution.
- A readable concept→definition→operation→counterexample→repair chain for the
  three core families, and a map showing actual checked connections versus open
  connections. No diagram is a substitute for the maps.
- Exact commands, hashes, executed checks, and coverage; real failures and unavailable
  original sources. Distinguish assistant development from this Cursor run.
- At least one positive reuse example, one operation-induced insufficiency example,
  one successful coherent transport, and one state/operation-admission example.
- A local rebrief addition with links that prevents these concepts disappearing
  again. Preserve the registered but deferred concepts.
- A provisional publication-placement note with claim-sized decisions, not a
  mandate to publish one large new paper.

Editorial options to consider AFTER the evidence:

1. Explanatory addendum/companion for recovered material and its source chain.
2. A worked operational chapter or case study within Absolute Distinction, if it
   demonstrates how inspecting distinctions changes the method, not merely an
   existing loss-of-information theorem in new vocabulary.
3. A separate methods paper only if the connected mechanism and its evaluations
   justify a coherent independent contribution. A title could be “Relational Units,
   Operational Boundaries, and Distinction-Preserving Reuse”; this is a working
   label, not a claimed result or a publication decision.
4. Several destinations, with one shared technical source and cross-references.

The Absolute Distinction owning thread retains control of its paper and existing
studies. Supply a proposed bridge, not a replacement definition or competing launch.

**Stop condition:** the first integrated prototype has explicit mathematical scope,
executed positive/negative controls, and an honest return report. Do not run more
variations merely to increase PASS counts. If a component remains unavailable or a
proposed adapter fails, return the working portion and precise open seam in this
same assignment. Do not finish with only a plan for another run.
